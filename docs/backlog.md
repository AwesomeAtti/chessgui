# Backlog

> The system of record for **all** work: features, bugs, technical debt, and rejected ideas.
> Append-only — nothing is deleted. Items change status; they don't disappear.

## Conventions

- **ID:** sequential, never reused (e.g. `B-001`).
- **Type:** `feature` · `bug` · `tech-debt` · `chore` · `idea`
- **Priority:** `P0` (now) · `P1` (soon) · `P2` (later) · `P3` (someday)
- **Status:** `proposed` · `approved` · `in-progress` · `done` · `deferred` · `rejected`

Deferred ideas worth keeping but not scheduling can also live in `docs/parking-lot.md`.

## Items

| ID | Type | Priority | Status | Description | Notes |
|-------|---------|----------|-------------|-------------|-------|
| B-001 | chore | P0 | done | Initialize repo with AI-dev starter kit | Methodology, prompts, handover, backlog in place |
| B-002 | chore | P0 | done | Stage 0 — one-page product vision | Approved and committed as `docs/product-vision.md`. Gate passed |
| B-003 | chore | P0 | done | Decision Gate: UI framework / desktop shell | **Tauri 2** + TypeScript frontend. See ADR-0001. Frontend stays Electron-portable via a single IPC adapter module |
| B-004 | chore | P0 | done | Decision Gate: local storage / database engine | **SQLite. See ADR-0004.** Also **downgraded from hard stop to notify-and-proceed**: PGN files are retained as source of truth and the MVP is read-only (B-050), so the DB holds nothing that exists only there — abandoning it costs a re-import, not data. Gate re-hardens at **B-015** (annotations), when that stops being true. Conditions: DB stays derivable, import idempotent, verbatim PGN in the row, stable row IDs from the first migration. Schema questions B-058 – B-060 are *not* downgraded — decide at B-054 |
| B-005 | chore | P1 | done | Decision Gate: chess rules/PGN library vs. own implementation | **Buy jobs 1–2, build job 3.** Rust `pgn-reader` + `shakmaty` for import; TS `chessops` for the open game; `chessground` for the board; no third-party Rust game tree. See ADR-0003 |
| B-006 | chore | P1 | proposed | Decision Gate: engine process management & UCI transport | Hard stop if it becomes a platform-surface commitment (bundled engine, sandboxing, code signing) |
| B-007 | feature | P0 | done | Done (session 8) — PGN import from paste, file picker, and drag-drop, all four milestones. Governed by ADR-0009 (strict import: a game imports or errors, nothing repaired). Full session-by-session build history, including the pgn-reader/chessops divergence findings, in `docs/backlog-archive.md`. |
| B-008 | feature | P0 | in-progress | In-progress — milestones 1 (sort) and 2 (column visibility) both done. Milestone 1: TanStack Table (`@tanstack/react-table` ^8.21.3, MIT) drives column-header click-to-sort in `LibraryView`. Pinned to the v8 line deliberately — v9 (npm `latest`) is a recent, more complex feature-flag architecture; v8 is the established, widely-documented API (AGENTS.md "industry standards first"). Milestone 2: right-click any header opens a checklist of hideable columns (TanStack's built-in `columnVisibility` state, no new dependency); `White`/`Black`/`Result` are locked visible (`enableHiding: false`), which also guarantees a header is always right-clickable even with every optional column hidden, so there's no separate "more columns" affordance to build. **Mocked three structurally different options first** (toolbar button, table-corner icon, right-click header) per AGENTS.md; the owner's first pick (toolbar button, matching the web-app survey — MUI X, GitHub, Linear, Notion) was revised after asking whether a desktop app changes the calculus. It does: Explorer, Outlook, and desktop-style grids (AG Grid, DevExpress) all put this on the header itself via right-click, not a toolbar button — that's a native-desktop convention distinct from the web-SaaS one, and chessgui is the former. Verified via typecheck/check:i18n/build and a headless-Playwright walk (open menu, hide each optional column one at a time, confirm the locked columns and the menu itself still work with everything else hidden, Escape closes it) — **not yet looked at by the owner in the real app**, per the standing rule that visual work isn't verified until a human has. **Milestones 3–6 done too (session 13), built as one pass at the owner's request rather than one PR per item.** (3) Drag-to-reorder: dragging lives on the header's own click target, not a separate grip icon — the first mockup used a Notion/Trello-style grip, checked against Explorer/Outlook and corrected to match (they drag the header cell itself). No keyboard-accessible alternative built this pass, by the owner's explicit choice — flagged in the code as worth revisiting. (4) "Reset columns" — one item in the existing right-click menu, restores order/visibility/width by clearing all three pieces of state (empty state already meant "defaults" for each). (5) Default order is now Date first. (6) Per-column min/max width via TanStack's `columnSizing`: `Elo` (both), `Result`, `Date`, and `ECO` locked (`enableResizing: false`, size === min === max) as short fixed-format tokens; `White`/`Black`/`Event` stay resizable. Every default width checked headlessly for header-label truncation (`th.scrollWidth === th.clientWidth`, all eight), not eyeballed — the owner had asked for that explicitly. No new dependency throughout. Verified via typecheck/check:i18n/build and a headless-Playwright pass (drag, resize, the resize-lock/visibility-lock distinction, reset, and confirming a plain click still sorts). Committed as `0d44994` (and milestone 2's `8079f2e` before it). **The owner's first look at the real app (session 14) found two bugs neither sandbox pass caught:** drag-to-reorder didn't work at all (native HTML5 drag-and-drop conflicts with Tauri's own window-level native drag-drop, used for PGN file import — headless Chromium's synthetic input bypasses that conflict entirely, which is why it passed there), and resizing one column visually moved locked columns too (CSS `table-layout: fixed` redistributes any width slack across every column with an explicit `<col>` width when the table's own resolved width doesn't match their sum — "locked" isn't a concept CSS tables have). Both fixed session 14: reorder is now hand-rolled from `mousedown`/`mousemove`/`mouseup` instead of HTML5 DnD; column widths are now explicit pixel values computed in JS for the table and every `<col>` (via a `ResizeObserver`-measured container width), with `event` as the one designated auto-fill column, so there is never slack left for the browser to redistribute. Re-verified with an expanded headless-Playwright pass (reorder via synthetic mouse events, White-resize doesn't move Date, Event grows/shrinks correctly from its true rendered width, Event-resize-then-White-resize still doesn't move Date or reintroduce a shrink-ratchet) plus the full typecheck/check:i18n/build/test chain. Committed as a new commit on top of `0d44994`. **Reorder still didn't work after that fix (session 15) — the owner reported "I cannot drag columns to reorder" after re-testing, with resize/reset explicitly paused for that report rather than confirmed broken.** Root cause this round: `.th-sort` is a real `<button>`, and WebKit's native form controls implicitly capture the pointer on press, which can suppress `mousemove` dispatch to `window` entirely while held — headless Chromium doesn't reproduce this, and resize never hit it because its handle is a plain `<div>`, not a form control. Fixed by switching from Mouse Events to Pointer Events with explicit `setPointerCapture()`. That introduced a second bug caught before shipping (not by the owner): `setPointerCapture` pins the `pointerup` target to the drag's origin header, so the compatibility `click` event still fires there after a completed drag and would otherwise toggle its sort as a side effect — fixed with a `suppressNextClickRef` flag set on any drag past the movement threshold and consumed by the header's own click handler. Re-verified headlessly (reorder, plain-click-still-sorts, click-after-drag-no-longer-toggles-sort, resize/reset unaffected) plus the full typecheck/check:i18n/build/test chain — **but headless verification has now missed this exact interaction twice in a row, so this should be read as "didn't break anything else," not confirmation the real app works.** `main` is 7 commits ahead of `origin/main`, none pushed, reorder specifically not yet re-confirmed by the owner. Remaining: B-010's Option C composed-filter panel + chips, and B-085's still-open settings-storage decision (needed only once this column state should persist across restarts — it doesn't yet, by design, same as sort). | MVP core |
| B-009 | feature | P0 | proposed | Board + move list; play through a game, navigate variations | MVP core |
| B-010 | feature | P0 | proposed | Header search & filter (player, event, date, result, ECO) | MVP core |
| B-011 | feature | P0 | done | Done (session 9) — SQLite persistence, all four milestones, owner-verified (imported games survive quit/relaunch). Spec and acceptance checklist: `docs/feature-specs/b011-persistence.md`. Build and verification history in `docs/backlog-archive.md`. |
| B-012 | feature | P1 | proposed | Chess.com import (account/archive) | Post-MVP. **Shape decided in B-102:** walk `/pub/player/{u}/games/archives`, fetch each month's **JSON** (not `/pgn`), unwrap it, and feed each game's `pgn` field through B-007's pipeline; take `uuid`, `rules` and `accuracies` from the JSON. Dedupe on `uuid`. Practical notes from the real download: **a mandatory descriptive User-Agent** including a contact address, **serial requests** because parallel ones get 429'd, and **empty months are normal** — 7 of 20 archives returned `{"games":[]}`. There is no single-game endpoint; `getGameById` in the popular npm wrapper is not part of the official API. Original note: | API rate limits and pagination |
| B-013 | feature | P1 | proposed | Lichess import (account/study/PGN export) | Post-MVP |
| B-014 | feature | P1 | proposed | UCI engine integration — attach engine, analyse position, show PV/eval | First post-MVP milestone |
| B-015 | feature | P1 | proposed | Annotations: comments, NAGs, variations, editing a game | Requires PGN write path |
| B-016 | feature | P1 | proposed | Create games manually (enter moves, set up position/FEN) | |
| B-017 | feature | P1 | proposed | PGN export (single game, selection, whole database) | Data-ownership commitment; pairs with import |
| B-018 | feature | P1 | proposed | Position search (find games reaching a position / pattern) | Expensive; needs position index (B-042). Design before building |
| B-019 | feature | P2 | proposed | Full-game engine analysis pass (blunder detection, accuracy) | |
| B-020 | feature | P2 | proposed | Collection-level analysis (opening repertoire, results by ECO, trends) | From vision "over time" list |
| B-021 | feature | P2 | proposed | Advanced search: boolean/saved queries, material & endgame filters | |
| B-022 | feature | P2 | proposed | Game management: tags, collections/folders, merge, dedupe, batch edit | **Dedupe landed here by decision, not by omission (ADR-0008 addendum, B-113).** Rule 6's content hash was removed from the MVP because derivability needs only drop-and-rebuild, so nothing in B-007 deduplicates and importing one file twice produces duplicate rows — visible and removable, which is the failure mode the ADR preferred over a silent false merge. Whatever is built here inherits three things already worked out: a key over the *normalised* game rather than the bytes (`fixtures/pgn/dedupe-*.pgn` are the specification, and demonstrate a re-export that must still match), **the variant must be part of any identity** since the Seven Tag Roster does not carry it, and API-sourced games key on `uuid` instead (B-102). `scripts/survey-pgn.mjs` already computes and reports collisions, so the evidence exists before the feature does |
| B-023 | feature | P2 | proposed | Opening explorer / tree view over the user's own games | |
| B-024 | feature | P2 | proposed | Design system: tokens, dark/light themes, board & piece theming | Vision's "modern" claim depends on this being deliberate |
| B-025 | feature | P3 | proposed | Play against an engine | Explicit "over time" item; not a play platform |
| B-026 | feature | P3 | proposed | Fair-play analysis | Explicit "over time" item. Ethically sensitive — needs its own spec |
| B-027 | feature | P3 | proposed | Endgame tablebase lookup (local or online) | |
| B-028 | feature | P3 | proposed | Opening-book (.bin/Polyglot) support | |
| B-029 | feature | P3 | proposed | Import from ChessBase formats (.cbh/.cbv) | Proprietary, poorly documented — high effort |
| B-030 | feature | P3 | proposed | Training / spaced-repetition on own mistakes | Adjacent product; keep out of core |
| B-031 | chore | P2 | proposed | Open-source readiness: licence, CONTRIBUTING, issue templates, public repo hygiene | Stated intent is to open source for the chess community |
| B-032 | chore | P2 | proposed | Packaging & distribution per platform (installer, signing, notarisation, auto-update) | Cost and friction often underestimated |
| B-033 | tech-debt | P2 | in-progress | In-progress — import performance is measured and fine (3,000 games is about 95ms, 10,000 is about 340ms end-to-end; the IPC payload, not parsing, was the cost, and B-011 fixed that by fetching PGN per-game). **Still open: rendering 10k rows and the <200ms filtered-search target**, which arrive with B-008/B-010. Full measurement method in `docs/backlog-archive.md`. |
| B-040 | feature | P2 | proposed | Reference database: millions of master games, read-mostly, bulk-loaded | Post-MVP. Separate store from the personal DB — different shape, different access pattern. Constrains B-004 by requiring it not be designed out |
| B-041 | feature | P2 | proposed | Opening explorer over reference DB: move frequencies + W/D/L statistics | The reason B-040 exists. Depends on B-040 and B-042 |
| B-042 | tech-debt | P2 | proposed | Position index design (Zobrist-style hashing, dedupe, storage cost) | Shared foundation for B-018 and B-041. Spec before building either |
| B-043 | chore | P0 | proposed | Source data for reference DB: find a masters game set with redistributable licensing | Blocks B-040. Licensing and download size are the real constraints, not the code |
| B-044 | chore | P0 | done | git init, secrets hygiene, per-repo pseudonymous commit identity, push to GitHub | Initial commit `99a6079` pushed to `origin/main`. Identity verified pseudonymous; `.DS_Store` excluded |
| B-045 | chore | P2 | proposed | GitHub repo hygiene: branch protection, issue/PR templates, CODEOWNERS, Dependabot | Do when the repo goes public, not before |
| B-046 | chore | P1 | done | CI on GitHub Actions: build + test matrix across Windows, macOS, Linux | **Green on all three platforms**, plus the frontend guardrails and `cargo fmt`/clippy. Took three runs. **It paid for itself immediately:** run one failed on Windows alone with a missing `icons/icon.ico`, which macOS and Linux do not need and no developer machine could have surfaced. That is precisely the divergence this item exists to catch, found on day one instead of by the first Windows tester (B-070) a year from now. `fail-fast: false` is what made it obvious the break was platform-specific rather than universal. **Promoted from P2 by B-068.** | Now the primary guardrail: shipping macOS-first is only reversible if the other two keep *compiling*. GitHub's Windows and Linux runners are free, so build-level divergence gets caught at zero cost even though interactive testing is deferred |
| B-047 | chore | P3 | proposed | Distribute releases via GitHub Releases (installers per platform) | Distribution channel decision is separate from repo hosting; revisit at B-032 |
| B-048 | chore | P0 | done | Spike: board render + move interaction while a UCI engine floods stdout | **Passed — ADR-0001 stands.** Frame time median/p95 17.0 ms (vsync) *identical* engine-stopped vs engine-running; worst frame was lower under load. Events pinned at 10.0/sec, zero drops. Findings in `docs/tech-stack.md`. **Main finding is that the premise was wrong:** Stockfish bursts (~44 lines/sec peak, MultiPV 8) then goes near-silent at depth — no sustained flood. Spike code discarded. Linux half remains B-066 |
| B-066 | chore | P2 | deferred | Repeat the B-048 spike on Linux / WebKitGTK | The actual close-out of handover risk 1. Deferred by B-068 (macOS-first). Until it runs, ADR-0001's Linux assumption is untested, not validated |
| B-068 | chore | P0 | proposed | Release sequencing: all three platforms remain the target; release order gated by testing capacity | **Not a scope reduction.** Cross-platform stays a first-class goal per the vision. The constraint is *verification*, not architecture: only macOS can currently be tested, so macOS releases first. Windows and Linux release as testers become available. All three build in CI from day one (B-046) |
| B-069 | tech-debt | P0 | in-progress | In-progress — portability boundaries (`src/shell/platform.ts`, `src/shell/files.ts`, `src/shell/opener.ts`) keep Tauri APIs and platform differences out of components. File dialogs and the IPC guardrail are closed; menu-bar conventions and engine binary discovery remain open. Full history in `docs/backlog-archive.md`. |
| B-070 | chore | P1 | proposed | Recruit Windows and Linux testers | **The actual dependency behind B-068, and previously unrecorded anywhere.** Nothing else unblocks Windows/Linux release. Natural source is the chess community the project already intends to open-source to (B-031) — which gives public-repo timing a functional reason to happen earlier, not just an ideological one |
| B-071 | chore | P1 | proposed | Headless smoke tests on Windows/Linux in CI: app launches, DB opens, PGN imports | Reduces the tester dependency without removing it. Catches the crashes and missing-runtime failures that dominate first-run problems on an untested platform; cannot catch anything about how it looks or feels |
| B-067 | tech-debt | P1 | proposed | Throttle high-frequency backend→frontend streams in Rust, not the frontend | **Validated by B-048 and written up in `docs/tech-stack.md`.** Parse in Rust, keep latest state (overwrite, never append — flat memory by construction), emit on a fixed timer. Measured 10.0 events/sec with zero drops while source rate varied 0–44 lines/sec. Kept at P1 despite the flood being smaller than feared: it costs almost nothing and removes a class of failure. **Applies to engine output only — the import half is settled and the answer is no (B-033, session 8):** 3,000 games is ~95 ms end to end and 10,000 is ~340 ms, so a progress bar would flash and vanish, which is worse than none. If import ever needs it, the trigger is B-011's database write rather than the parser. Carry into `docs/architecture.md` when it exists |
| B-049 | chore | P1 | done | Closed — superseded by B-114/ADR-0009. Historical policy debate (ADR-0008, tiered fidelity) in `docs/backlog-archive.md` and `docs/adr/`. |
| B-050 | chore | P1 | done | Decide whether MVP is read-only or writes annotations | **MVP is read-only.** Its job is to validate the import path and the look and feel; neither needs writes. Vision §4 and open question 1 amended. **Condition attached:** read-only is a build-order choice, not a schema shape — stable row IDs, verbatim PGN as source of truth, header columns strictly derived, so B-015 can be added without migration. Simplifies the B-004 gate: no edit/drift path to design for at MVP |
| B-051 | chore | P1 | proposed | Decide whether to bundle an engine (e.g. Stockfish) or require user-supplied | Vision open question 3. Distinct from B-006: that is transport, this is licensing, binary size, signing, per-platform builds |
| B-052 | chore | P2 | done | Decide open-source timing and licence | **GPL-3.0-or-later.** Forced by the chess stack (shakmaty/pgn-reader/chessops/chessground all GPL-3.0+), and aligned with stated intent. See ADR-0002. Public-repo *timing* still open — tracked under B-031 |
| B-057 | chore | P0 | done | Add `COPYING` — full, verbatim GPL-3.0 text | Fetched from the FSF and verified: 674 lines, 35,149 bytes, correct header and closing paragraph. In place before any GPL dependency was added |
| B-065 | chore | P1 | done | Add SPDX identifiers (`GPL-3.0-or-later`) to `Cargo.toml` and `package.json` | Present in both manifests from the commit that created them, so no window existed where the repo carried code without a declared licence. `tauri.conf.json` also points `bundle.licenseFile` at `COPYING` |
| B-058 | tech-debt | P1 | done | Player identity: separate `players` table with FK, plus name normalisation | **Decided in ADR-0005**, expressed as `Player` in `src/model/game.ts`. `normalisedName` is derived and lossy; the verbatim name stays authoritative. The migration itself is B-011; identity merge is B-022. Original note: | Case, accents, `Lastname, Firstname` vs `Firstname Lastname`. Needed for "my Sicilians as Black" (B-010) and for dedupe/merge (B-022). More work up front, much less pain later |
| B-059 | tech-debt | P1 | done | Partial-date handling: store raw PGN date string *and* a nullable parsed date | **Decided in ADR-0005** as `PgnDate` — `raw` authoritative, plus derived `parsed`/`year`/`month` so year-only dates still sort. Formatting goes through `src/i18n/format.ts` using `Intl`, never string concatenation. Original note: | Real PGN routinely carries `2024.??.??` or `????.??.??`. Sorting/filtering needs the parsed value; fidelity needs the original |
| B-060 | tech-debt | P1 | done | Store the full PGN tag set as JSON alongside extracted columns | **Decided in ADR-0005.** Hot fields promoted to indexed columns and strictly derived; `tags` retains everything; `result` is an integer (`1`/`0`/`-1`/`null`), never `"1-0"`. Original note: | Real files carry `Annotator`, `PlyCount`, `Variant`, `FEN`, `SetUp`, `WhiteTitle`, and site-specific tags. Extract the hot ones; keep everything so nothing is silently dropped. Store `Result` as an integer, not `"1-0"` |
| B-061 | idea | P3 | proposed | zstd-compress stored PGN text | Raw PGN compresses extremely well. Post-MVP; noted so it isn't rediscovered |
| B-062 | idea | P3 | proposed | SQLite FTS5 over player and event names for fuzzy search | Cheap once the store is chosen. Post-MVP |
| B-063 | tech-debt | P2 | proposed | Annual dependency upgrade chore: `pgn-reader` / `shakmaty` 0.x breaking releases | pgn-reader's author describes maintenance as minimal, following shakmaty. Pin versions; expect one upgrade pass per year |
| B-064 | tech-debt | P3 | deferred | Deferred P3 — for the MVP only one library (chessops) walks moves, so there is nothing to keep consistent. Returns when the position index (B-018/B-042) or engine analysis (B-019) needs both. Full reasoning in `docs/backlog-archive.md`. |
| B-053 | chore | P0 | done | Stage 1 deliverable: core workflows document | **Written as `docs/core-workflows.md`**, ahead of the layout choice rather than after it. Six journeys (W1–W6). Central finding: the unit of work is not "one game" but *a question several games answer*, and three workflows make conflicting layout demands — W2/W5 want the list prominent, W3 wants the board dominant, W4 needs several games open at once. **It immediately paid for itself:** testing layout C against W2 exposed a scan → open → reject → next loop that tabs make expensive. Two fixes were tried in the library's side panel — a preview board, then a details panel — and both were removed; the preview showed the same starting position for every row, and the details duplicated the table's own columns while charging a fifth of the width. **The answer was that W2 needs nothing added**: the columns that identify a half-remembered game are already in the table, so the library tab is now full-width with no panel. Original note: | The one Stage 1 deliverable the vision does not already cover. 4–6 end-to-end user journeys (import → find → open → play through). Feeds the skeleton's screen list. **Promoted in practice by B-084:** it was skipped as "not on the critical path" and the skeleton immediately got its screen model wrong. Do it before B-007/B-008 rather than discovering the same thing again at a larger scale |
| B-054 | chore | P0 | done | Milestone M1 — Skeleton: window, board, game list, navigation | **Done and now fully verified — the app runs on macOS with no errors, and the secondary paths were walked by the owner in session 4.** Library table, tabs opening and closing, arrow-key move navigation with the move list following, window resize, Info tab, filter box, Enter-to-open, click-a-move, tab-switch position retention, footer move buttons, no document scrollbars at any window size. Home/End confirmed via fn+←/fn+→. **All eight secondary paths pass, and all three findings came from paths that passed** (M1-F1 – M1-F3 in `docs/milestones/m1-skeleton.md`): no key on the development keyboard is labelled `Home` or `End`, so the working binding is undiscoverable → widened B-086; Accel+W on the library tab falls through and closes the window, examined and **kept** as the macOS convention, with the early return in `App.tsx` now commented so it survives → M1-F2; and the live-resize stretch was chased with a control and turned out to be the webview's, not ours → B-096 rejected. The general point is the one the project keeps relearning: **every box was expected to pass, every box did pass, and walking them still returned three things — a tick is not the same as an absence of information.** **The layout took three attempts** and the third was chosen from a survey, a workflows document and mocked options rather than guessed; see ADR-0007 and B-087. Earlier note: **First product code exists.** `docs/milestones/m1-skeleton.md` written; Tauri 2 + React 19 + Vite scaffolded; board, game list and navigation built; guardrails B-069/B-072 and CI B-046 landed with it. Frontend verified green (`tsc --noEmit`, `vite build`, `check:i18n`). **Not done:** `cargo build` and the window opening are unverified — the AI sandbox has no Rust toolchain, so that half must be run in a real terminal |
| B-055 | chore | P1 | proposed | Create `docs/tech-stack.md` and `docs/architecture.md` | Stage 2. Written immediately after B-003/B-004 land, not before — they have nothing to say until then |
| B-056 | idea | P3 | proposed | Position setup by FEN paste as a cheap early board test | Falls out of B-016; useful as a skeleton-stage smoke test before any database exists |
| B-072 | tech-debt | P1 | in-progress | In-progress — i18n foundation: typed message catalogue, `Intl` formatting, Rust returns error codes only. Three of four guardrails landed; the pseudo-locale CI build (guardrail 3) is split out as B-081. Full history in `docs/backlog-archive.md`. |
| B-073 | tech-debt | P2 | proposed | Separate storage notation from display notation (localised SAN + figurine) | **The chess-specific half of i18n, and the part that would be missed.** SAN piece letters are language-dependent — `Nf3` / `Sf3` (de) / `Cf3` (fr). PGN mandates English SAN on disk, but real exports from German software do not always comply, so import must *tolerate* localised SAN (B-049 fidelity policy) while storage stays canonical English. Display then renders per-locale, with figurine notation as the escape hatch that sidesteps language entirely. Getting this wrong means notation strings leak into the schema |
| B-074 | tech-debt | P2 | proposed | Locale-aware collation, dates, and number formatting | Player-name sorting must use locale collation, not byte order — accents and non-Latin scripts sort wrongly otherwise. Ties directly to B-058 (name normalisation) and B-059 (partial dates): store canonical, format at the edge. Never hand-roll date strings |
| B-075 | chore | P3 | proposed | Decide which locales ship after English | Not urgent — B-072 makes it cheap whenever it happens. Natural first candidates follow the chess world rather than general market size: German, Spanish, Russian, French |
| B-076 | tech-debt | P2 | proposed | Re-test engine output rate under the *realistic* pattern: rapid position changes | B-048 ran one long analysis, which is the quiet case — Stockfish only floods during the shallow-depth phase. A real GUI restarts the search on every move, so clicking quickly through a game replays the burst continuously. That is the untested case, and it is the normal one. Cheap to fold into B-014 |
| B-078 | tech-debt | P3 | deferred | Keep the database derivable: rebuildable from PGN, source of truth retained | **Downgraded P1 → P3 and deferred (session 5), on the owner's objection that this is a production-level concern being carried by a development-stage app.** The objection is right, and the cost of it was real: derivability had accreted an owner, a gate that "re-hardens", conditions quoted in two ADRs, and a place in three separate documents — **for a fact that fits in one sentence. We do not delete the PGN files, so the database can be rebuilt by importing again.** Nothing enforces that, nothing needs to, and there is no user data to lose. Drop the database and re-import: that is the whole recovery procedure, and it is available today without anyone building it. **What was genuinely wrong with the earlier framing:** it treated "the DB might be abandoned" as a data-safety problem, which it is not while every game came from a file that is still there. That inflated ADR-0004's reasoning, inflated ADR-0008's, and made a "rebuild database from source PGNs" command look like a requirement rather than a convenience. **When this becomes real, and it does:** B-015 introduces annotations, which are the first data that exists nowhere else, and the concern arrives with actual users who could lose something. At that point an export path (B-017/B-079) is the answer, not a derivability doctrine. Until then this item should not be scheduled and should not be quoted as a constraint. Original note: | **Wording narrowed by ADR-0008's session-5 addendum (B-113), and the distinction is the substance of this item.** It previously said "re-importing the same PGN must not duplicate games", which conflated two different properties. **Derivability is: dropping the database and rebuilding it from the retained PGN reproduces the same database** — that holds with or without dedupe, because each source file is read once per rebuild, and it is the only part ADR-0004's gate downgrade actually rests on (nothing exists solely in the database). **Dedupe is the separate property of importing the same file twice into an existing database**, which the MVP no longer attempts — rule 6 is out, duplicate rows are permitted, visible and removable, and merging is B-022's subject. So the tested path this item owns is **drop and rebuild**, and a "rebuild database from source PGNs" command is still the cheapest way to keep it honest. Original note: **This is the condition ADR-0004 rests on, so it needs an owner rather than good intentions.** Re-importing the same PGN must not duplicate games; dropping the DB and rebuilding must be a supported, tested path. The moment this quietly stops being true, the storage gate re-hardens and nobody notices. A "rebuild database from source PGNs" command is the cheapest way to keep it honest |
| B-079 | chore | P1 | proposed | Build PGN export (B-017) alongside annotation (B-015), not after | Falls out of ADR-0004: annotations are the first data that exists only in the database, which is what re-hardens the storage gate. Export defuses it again by making the DB derivable-in-reverse. Shipping annotation without export creates a window where the user's only copy is in a schema we reserved the right to change |
| B-095 | chore | P2 | proposed | Replace the placeholder icons with real artwork, and generate the full set | Current icons are a generated checkerboard: `icon.png` (512) and `icon.ico` (BMP 16–128 plus PNG 256). They exist to make the build work, not to look like anything. Real bundling (B-032) additionally wants `icon.icns` for macOS and the assorted PNG sizes Windows Store and Linux desktop files expect — `tauri icon` generates the whole set from one source image. Pairs with B-024, since the app icon is part of the design system rather than a build artefact |
| B-094 | tech-debt | P2 | done | Verify the MSRV claim, or stop making it | **Closed session 6, and the answer was that the claim was already false — no compiling required to find out.** The dependency graph sets the floor: `pgn-reader 0.29.0` declares `rust-version = "1.88"` and the `shakmaty 0.30.1` it pulls in declares `1.95`, both read from their own manifests. So `1.77.2` had been untrue since session 5 added the crate, and a build on 1.77.2 would have failed at resolution rather than on our source. **Corrected to `1.95`**, which is what CI has pinned all along, so the declaration now matches the only toolchain anything is tested on. The AI sandbox can reach crates.io but not `static.rust-lang.org`, so installing an old toolchain to confirm by compiling was not possible — but it was also not necessary, because a declared MSRV below a dependency's is wrong on its face. **The transferable bit: the cheapest check was reading the dependencies' manifests, and nobody had.** Original note: `src-tauri/Cargo.toml` declares `rust-version = "1.77.2"`, inherited from Tauri 2's own requirement rather than measured. Everything is actually built and linted on 1.95.0, so **nothing has ever confirmed the code compiles on 1.77.2** — our own source may use newer features. This is the same shape as the risks this project has already been caught by: a number that looks like evidence and is really an assumption. Two honest fixes: add a CI job pinned to 1.77.2 running `cargo check`, or raise the declared version to one that is actually tested. The claim matters to distro packagers and contributors, so it should be true or absent |
| B-080 | chore | P1 | done | Add `cargo fmt --check` and `cargo clippy -D warnings` to CI | Added on the Linux runner only — formatting and lints are platform-independent, so running them three times just costs minutes. **Both already pass locally on 1.95.0 with no output**, so this guards against regression rather than cleaning anything up, which is exactly the condition this item was waiting for. Original note: | Deliberately left out of the B-054 CI workflow rather than forgotten. The AI sandbox has no Rust toolchain, so neither could be verified before committing, and a lint job that fails on its first run for cosmetic reasons teaches people to ignore CI. Add it immediately after the first successful local `cargo fmt` |
| B-081 | tech-debt | P1 | proposed | Pseudo-locale build in CI — the third B-072 guardrail | `npm run check:i18n` proves no *literal* escaped into a component; it cannot prove a translated string fits. A pseudo-locale (accented, ~35% longer, bracketed) run against the built UI catches both the strings that never reached the catalogue and the layouts that clip when German arrives. Needs something to screenshot or assert against, so it naturally follows B-024 |
| B-082 | chore | P1 | done | Pin the toolchain and commit `Cargo.lock` | `Cargo.lock` committed; CI switched to `cargo build --locked`; the CI toolchain pinned to **1.95.0** instead of floating `@stable`. **`rust-toolchain.toml` was deliberately not added.** The dev machine runs Homebrew Rust, which has no `rustup`, so that file would be honoured in CI and silently ignored locally — the precise opposite of a pin's purpose, which is making the two agree. Pinning the CI side is the half that is actually available. **Consequence to remember: the pin is now one-directional.** Bump the CI version deliberately when the local toolchain moves, or they drift apart again and CI stops representing the developer machine. Installing `rustup` (and removing Homebrew Rust) would make both halves possible — worth doing if toolchain differences ever bite. Related: B-094 | `Cargo.lock` cannot be generated in the AI sandbox (no cargo), so the first real `cargo build` produces it and it must be committed — it is an application, not a library. Pinning the toolchain makes the three CI runners agree with the developer machine, which is exactly the class of divergence B-046 exists to catch. Once both exist, switch CI to `cargo build --locked` |
| B-083 | idea | P3 | proposed | Revisit routing when the screen count grows | M1 navigates with a `useState` discriminated union in `App.tsx` — two screens do not justify a router and a desktop app has no URL bar to satisfy. Recorded so the choice is visible rather than inherited. Revisit around B-010/B-014, when there are four or more screens and back/forward starts to mean something |
| B-084 | tech-debt | P0 | done | Rebuild the shell as a desktop master–detail layout | **The two-screen model B-054 specified was wrong**, and running the app is what revealed it — it "felt like a web page, not a desktop app". A chess database is master–detail: the game list is permanent furniture and selection populates the board beside it, rather than replacing the screen. Rebuilt as one window: resizable library pane, board pane, move pane; the window is the viewport; only pane bodies scroll. Fixed three symptoms — rows reflowing on narrow windows, the board ignoring available space, and list and board never visible together. **Root cause is B-053 being skipped**, which is the document that was supposed to produce the screen list |
| B-085 | feature | P2 | proposed | Persist window geometry, pane sizes, and now library column layout between sessions | **Scope grew twice.** First with B-107: if component windows are detachable, this has to persist *per window* keyed on a stable window identity (B-110), not just the main window's splitter — and for a streamer, a popout that forgets where it was is the difference between a scene that works on launch and one that has to be rebuilt. Second with B-008's milestones 3/4/6 (session 12): column order, visibility, and widths in `LibraryView` want the identical "small bit of UI state, not derivable from PGN, not the games database" treatment — no reason to solve settings storage twice. **Still an open, unmade decision**, not a hard-stop gate (SQLite as the *database* is already ADR-0004; this is a smaller, reversible choice — a local settings file vs. Tauri's official store plugin (MIT) — AGENTS.md's "notify and proceed" tier). Original note: The splitter resets to 420px on every launch, which is the kind of small thing that makes an app feel unfinished. Needs somewhere to keep UI state — deliberately *not* the games database (ADR-0004 keeps that derivable, and window geometry is not derivable from PGN). Tauri's store plugin or a small settings file. Bundle with other preferences rather than doing it alone |
| B-093 | feature | P1 | in-progress | Mainline move navigation | Not in B-054's original scope, added because the layout could not be judged with a board that never moves, and W3 makes stepping through moves the most repetitive interaction in the product. `src/features/game/mainline.ts` walks the PGN with chessops into one FEN per ply; arrow keys, Home/End, and clicking a move all navigate. **Now covered by eleven committed tests** (`mainline.test.ts`, B-106) — the session-3 checks below were run by hand and thrown away, which verified a moment rather than the code. Two of the newly written cases initially failed, and **both failures were bad fixtures of mine rather than walker defects**. Earlier note: **Verified against fixtures** including a FEN/SetUp header, castling, and an illegal move mid-game (truncates rather than throwing). **Variations, comments and NAGs remain B-009** — this is the mainline only, and the real game tree should replace it without changing the board or panel components |
| B-086 | feature | P2 | in-progress | Keyboard navigation for the game list | Arrow keys move the selection and Enter opens the selected game in a tab, both landed with ADR-0007. Accel+W closes the active tab, routed through `src/shell/platform.ts` so the Cmd-versus-Ctrl assumption is made once (B-069). **Widened at the M1 verification pass (session 4) into owning the whole shortcut scheme, and given a concrete finding to start from: no key on the development keyboard is labelled `Home` or `End`.** The bindings themselves are confirmed working, via fn+←/fn+→ — which is how Apple keyboards without a numeric keypad emit those keys. So the code is right and the shortcut is still, for practical purposes, undiscoverable: it is reachable only by someone who already knows the fn convention. It was inherited from Windows chess software without anyone checking the target keyboard has the keys — a B-069 portability miss in a place nobody thought to look, since keyboards are not usually filed under "platform surface". Candidate fix is Cmd+←/Cmd+→ as aliases via `platform.ts`, **deliberately not applied yet**: the owner's call was to choose the bindings as one coherent scheme rather than spot-fix a key at a time, because piecemeal additions are exactly how a scheme becomes inconsistent. Not urgent — the ⏮/⏭ footer buttons already make first/last reachable. **Still open:** the scheme itself, Home/End reachability, Tab-order review, a discoverable shortcut reference. Original note: | Arrow keys to move the selection, Enter to open, Home/End to jump. Rows are currently mouse-only, which is acceptable for a skeleton and not for an application people use daily. Shortcut modifiers must go through `src/shell/platform.ts` (B-069), never `metaKey` directly |
| B-090 | tech-debt | P1 | proposed | Decide narrow-window behaviour below ~900px | A fixed 320px panel plus a fluid board plus a multi-column table stops working somewhere around 900px, and nobody has looked. Options: collapse the panel to icons, drop table columns by priority, or set a minimum window size and be honest about it. Cheap to decide, embarrassing to discover in a screenshot from a tester |
| B-091 | tech-debt | P2 | proposed | Cap the board's maximum size and decide what absorbs surplus width | A fluid board with no ceiling is absurd on a 32-inch monitor. Once capped, the leftover width becomes margin around a centred board, or a wider panel. Chess.com centres. Pairs with B-089 |
| B-092 | feature | P2 | proposed | Focus mode — hide everything except the board | Chess.com has this (plus Theatre mode) and it is the logical end of B-089's collapsibility. Cheap, and it directly serves W3, the review workflow where nothing but the board and moves matters |
| B-087 | chore | P0 | done | Choose the application layout from mocked options, before coding it | **Decided as ADR-0007 — layout C+**, after four structural options and two refinement rounds were mocked and discussed rather than coded. Inputs: `docs/ui-survey.md` (the category makes layout configurable because it is genuinely subjective), `docs/core-workflows.md` (W2/W5 want a wide list, W3 wants a dominant board, W4 wants several games open), and chess.com screenshots supplied by the owner (fluid board, fixed panel, three-part panel structure). Option E — one board with the library inside the panel — was seriously considered and rejected because a 320px panel cannot hold a filterable multi-column table. **State is modelled as a list so E stays reachable as a shell change.** Original note: | Two coded layouts were rejected in one session, which is what prompted the process change. `docs/ui-survey.md` written: the decisive finding is that **every mature product in this category makes layout user-configurable** — ChessBase ships predefined plus savable custom layouts, Scid vs. PC has five slots, and Lichess's fixed layout is its most-complained-about trait. Four options mocked (master–detail, two contexts, tabbed documents, board-dominant). **Awaiting a choice.** Plan is one default built properly, with the others as presets later — not a dockable pane system |
| B-088 | chore | P1 | proposed | Ongoing — backport general (non-chess-specific) lessons to the AI-dev starter kit as they are found. Fourteen lessons recorded so far — full list in `docs/backlog-archive.md`. **Session 9 adds a fifteenth: give `docs/handover.md`/`docs/backlog.md` a built-in archive/rotation mechanism from the start, rather than discovering the bloat at 250+ KB (see B-118).** |
| B-089 | feature | P2 | proposed | Layout presets, and collapsible panels | Falls directly out of `docs/ui-survey.md`. Two or three named presets capture most of the value of full configurability for a fraction of the cost of a dockable pane system. **Collapsibility is the higher priority of the two**: the single loudest complaint about the newest layout in this category is a panel that steals board space and cannot be hidden. Ties to B-085 (persisting pane sizes) |
| B-097 | feature | P2 | proposed | Shrank to near-nothing under ADR-0009 — an import error is at most one per input, so the report just needs to name the game and say everything after the failure was never read. Design details in `docs/backlog-archive.md`. |
| B-102 | chore | P1 | done | Done — chess.com import derives hot fields from the PGN tags inside the monthly JSON, dedupes on `uuid`, stores both `eco` and `ecoUrl`. Full measurement writeup in `docs/backlog-archive.md`. |
| B-105 | feature | P2 | proposed | Proposed — opening names must come from an ECO-to-name table, never by prettifying the chess.com `ECOUrl` slug (it destroys punctuation). Full trap writeup in `docs/backlog-archive.md`. |
| B-103 | tech-debt | P2 | proposed | Proposed — chess.com's per-player result strings need mapping to ADR-0005's integer `result`, but the draw vocabulary is unobserved (sample had no draws). Owner-approved rule: derive the winner positively, never default to draw. Full note in `docs/backlog-archive.md`. |
| B-104 | chore | P1 | deferred | Deferred to B-012 — `whiteAccuracy`/`blackAccuracy` columns were added then removed from the model since the MVP (PGN-only) can never populate them. Reasoning in `docs/backlog-archive.md`. |
| B-100 | feature | P2 | proposed | Reduced to 'select the variant named in the tag, on both sides' under ADR-0009 rule 4. `shakmaty`'s `variant` feature and `chessops` both handle the eight standard variants. Full history in `docs/backlog-archive.md`. |
| B-101 | chore | P1 | in-progress | In-progress — `scripts/survey-pgn.mjs` is built and verified; the chess.com corpus is measured (clean, zero anomalies). **Still waiting on the owner: run it against the loose/hand-typed PGN files**, which is what gates B-116. Full method and chess.com results in `docs/backlog-archive.md`. |
| B-098 | tech-debt | P2 | rejected | Rejected — ADR-0009 makes non-English SAN an error rather than something to interpret; superseded by B-115. Full reasoning (why the naive fix is unsafe) in `docs/backlog-archive.md`. |
| B-106 | chore | P0 | done | Test harness: vitest for TypeScript, `cargo test` for Rust, one shared `fixtures/` corpus | **Raised and done in session 5, because B-099 could not be started without it.** The finding that produced this item is worth more than the item: **four sessions in, this project had no test runner of any kind** — no vitest, no `*.test.ts`, no `#[cfg(test)]`, no `cargo test` in CI, no fixtures directory. It did not read that way from the handover, which described both `mainline.ts` (B-093) and `survey-pgn.mjs` (B-101) as "verified against fixtures". Both were, by throwaway scripts run once and discarded — **verification of a moment rather than of the code**, and indistinguishable from the real thing in prose. B-099 meanwhile reads as ready to start and silently assumed a harness on both sides of ADR-0008 rule 3. Same shape as the five plausible-explanation corrections in session 4, in a new place: a backlog item that looks ready and rests on something nobody checked. **Notify-and-proceed** — vitest 4 is MIT, a dev dependency, conventional, and reversible in an afternoon. Recorded in `docs/tech-stack.md`. Delivered: `npm test`/`npm run test:watch`; `test` block in `vite.config.ts` (from `vitest/config`, one config file so the `@` alias is defined once — tests must resolve imports exactly as the app does); `environment: "node"` and `globals: false`; **eleven committed tests for `mainline.ts`**, being session 3's discarded checks made permanent (standard walk, `FEN`/`SetUp` header, castling, no result token, illegal move truncating at the right ply, unusable start position, empty input, `clampPly`); `src-tauri/tests/fixtures_contract.rs`; `npm test` in the CI frontend job and `cargo test --locked` on **all three** platforms rather than Linux-only, because the Rust test asserts a relative path resolves and that is a B-069 category of divergence. **Two negative controls run, and both failed as intended:** an intentionally illegal move in the `clean` fixture failed its case, and a broken corpus path failed loudly at load rather than passing vacuously. **Writing the two tests found two bugs — both in my own fixtures, neither in the walker** (a castling fixture that moved the rook first, and an endgame FEN claiming castling rights it could not have). That is session 3's "verify the fixture before believing the failure" reproduced exactly, and it is the argument for committing tests rather than running them once: the throwaway version would have recorded a walker bug that does not exist | **The Rust half was unverifiable in the sandbox and has since been run by the owner: `cargo fmt` reformatted one statement and `cargo test` passes (1 test).** `Cargo.lock` did not change, because `serde_json` was already in the dependency graph, so the duplicate `[dev-dependencies]` entry is free and `--locked` still holds in CI. **`cargo clippy --all-targets -- -D warnings` also run and clean**, so every gate CI applies has now been faced locally: fmt, clippy, cargo test, plus the frontend's typecheck, guardrails, build and `npm test`. Nothing in this item is "should work". **One late correction worth keeping, because the test passed and was still wrong.** Re-run after B-099 grew the corpus from two fixtures to eighteen, `fixtures_contract.rs` reported the identical single green test — its only structural assertion was that the manifest is *non-empty*, so it could not distinguish two entries from eighteen. **A test blind to a 9× change in its own input is a green light carrying no information**, which is the same failure as a throwaway script called a suite, one level further in. Now asserts the manifest count equals the number of `.pgn` files on disk, mirroring the TypeScript side, so an unlisted fixture — read by nothing, asserted by nothing — fails loudly. **Small note on the formatting guess, kept because it is the reusable part:** writing Rust that must satisfy `cargo fmt --check` without being able to run rustfmt meant predicting where it would break long chains, and the prediction was wrong in exactly one place — it broke after the `=` rather than inside the chain. So the working rule for AI-written Rust in this repo is not "format it correctly", it is **"expect one `cargo fmt` pass and budget for it"** |
| B-099 | chore | P1 | done | Done — 18-fixture corpus shared between Rust and TypeScript, covering ADR-0008/0009's rules. Findings (localised SAN silently reinterpreted, variant tag honoured, unsupported variant indistinguishable from a broken position) are in `docs/backlog-archive.md`. |
| B-114 | chore | P0 | done | Done — ADR-0009 (strict import) supersedes ADR-0008. Four rules replace seven; the libraries are the validator and nothing is added on top. Full decision record in `docs/backlog-archive.md` and `docs/adr/`. |
| B-113 | chore | P1 | done | Done — ADR-0008 addendum: variants are selected explicitly on both sides (rule 3b rewritten), and rule 6 (content-hash dedupe) removed from the MVP. Full reasoning in `docs/backlog-archive.md`. |
| B-096 | tech-debt | P2 | rejected | Rejected — the live-resize stretch was the webview's own compositing, not our code (confirmed with a control: the library tab, which has no JS-sized element, does the same thing). Full writeup in `docs/backlog-archive.md`. |
| B-107 | feature | P3 | proposed | Proposed, P3 — detachable component windows (board, clock strip, eval bar, move list, info) so OBS can window-capture rather than screen-region-capture. Priced by B-112. Full writeup in `docs/backlog-archive.md`. |
| B-108 | chore | P3 | proposed | Proposed, P3, hard-stop tier — must precede any B-107 code. Two questions: window creation belongs behind `src/shell/`; cross-webview state sharing is a real architectural decision (read-only projection windows are the cheap option). Full writeup in `docs/backlog-archive.md`. |
| B-109 | feature | P3 | proposed | Proposed, P3 — broadcast-friendly window properties (chroma key, borderless, aspect lock). True transparency on macOS may require `macos-private-api`, which may affect App Store eligibility — unverified, flagged for confirmation before promising it. Full writeup in `docs/backlog-archive.md`. |
| B-110 | tech-debt | P3 | proposed | Proposed, P3 — window titles must be stable identifiers, not descriptions, because OBS binds a capture source to window title and a changing (or localised) title silently breaks a configured scene. Full writeup in `docs/backlog-archive.md`. |
| B-111 | chore | P3 | proposed | Proposed, P3 — window capture is platform-divergent (Wayland/PipeWire on Linux is the hard case) and sits at the intersection of the two things this project cannot currently test (B-066, B-070). Full writeup in `docs/backlog-archive.md`. |
| B-112 | chore | P3 | proposed | Proposed, P3 — open vision question: are chess streamers an audience this product serves? Prices B-107 to B-111; nothing in that group blocks the MVP either way. Full writeup in `docs/backlog-archive.md`. |
| B-115 | tech-debt | P2 | proposed | Known issue, P2 — a non-English-SAN file imports as a game nobody played, silently, because `pgn-reader` and `chessops` fail differently rather than erroring. Fix (token-count comparison, not language detection) and fixtures noted in `docs/backlog-archive.md`. |
| B-117 | tech-debt | P2 | done | Done (session 8) — external links (chess.com `ECOUrl`) open via `src/shell/opener.ts`, not a bare anchor. Needed **both** `opener:allow-open-url` and `opener:allow-default-urls` capabilities; either alone builds green and does nothing. `Link` tag in the all-tags disclosure remains plain text (B-121). Full story in `docs/backlog-archive.md`. |
| B-116 | tech-debt | P3 | deferred | Import resynchronisation: carry on after a refused game instead of stopping | **Raised and deliberately not built, session 6, and the deferral is the decision.** Measured at B-007 milestone 2: `pgn-reader`'s errors are irrecoverable, so an unterminated `{` near the top of a large export loses every game after it. Recovery would be a forward scan for the next `[Event ` at a line start, restarting the reader there — perhaps fifteen lines. **It does not violate ADR-0009 rule 3**, since every recovered game is still parsed in full by the library; what it adds is our own judgement about where a game *begins*, permanently, for an event **nobody has counted**. **B-101's un-run half is that count, and it is the trigger for this item**: if loose files contain unterminated comments, build it; if they do not, this stays deferred forever. Pinned meanwhile by `the_games_after_an_unterminated_comment_are_lost_and_that_is_measured` in `src-tauri/src/import/mod.rs`, so whoever changes their mind has to change a test that says why. Precedent it follows: the same "cheap to add later, expensive to remove" asymmetry that deleted ADR-0008's content hash |
| B-118 | tech-debt | P1 | done | **Done (session 9) — this item.** `docs/handover.md` and `docs/backlog.md` were bloated (147KB/129KB) with accumulated session narrative. Fix applied: full session-by-session narrative moved verbatim to `docs/session-archive.md`; verbose per-item investigation logs in this file moved to `docs/backlog-archive.md`, replaced here with short current-state summaries and a pointer. Nothing was deleted. `ai/methodology.md`'s documentation system section updated to name the two archive files as read-when-needed rather than every-session. Also fixed in passing: B-117's row had been split across nineteen physical lines by a stray line-wrap, silently breaking the markdown table (invisible when rendered, visible only in a diff or a script) — collapsed back into one row. See B-088 for the starter-kit backport of this lesson. |
| B-119 | chore | P2 | proposed | Proposed — render the UI headlessly (Playwright) and screenshot it, so visual review does not depend entirely on the owner. Already used ad hoc in sessions 8-9 and found real faults a green build could not. Full writeup in `docs/backlog-archive.md`. |
| B-120 | feature | P3 | proposed | A drag-over affordance, so a drop looks possible before it happens | Milestone 4 handles Tauri's `drop` and ignores `enter`/`over`/`leave`. So the window gives no feedback while files are held over it, and the only confirmation that a drop is possible is the dialog appearing after the fact. The dialog is the *decision* surface and that part is right; this is the smaller question of whether the window looks receptive. Cheap — the events are already delivered by the same subscription — and deliberately deferred rather than forgotten, because it is polish and the drop itself works |
| B-121 | tech-debt | P3 | proposed | Make URLs inside the All-tags disclosure open too | B-117 routed the info panel's `ECOUrl` through `src/shell/opener.ts`. The disclosure below it still renders every tag as plain text, so chess.com's `Link` — the URL of the actual game, arguably the more useful of the two — is copyable but not clickable. Left alone on purpose: the disclosure is a raw-tag dump where every value is untyped text, and deciding *which* of them are URLs is a small classifier that wants a moment's thought rather than a regex bolted on at the end. The opener already refuses non-`http(s)` schemes, so the safety half is done |
| B-122 | feature | P3 | proposed | Proposed, P3 — the single-game import flow (drop, dialog, Import, offer to open) may be one step longer than it needs to be. Feel question, owner's call, deliberately not decided from the armchair. Full writeup in `docs/backlog-archive.md`. |
| B-123 | bug | P1 | done | Done (session 8) — pasting PGN on a game tab did nothing, because the paste listener was scoped to `LibraryView`. Moved to `App.tsx`. Full writeup in `docs/backlog-archive.md`. |
| B-124 | chore | P2 | proposed | Proposed — point people testing the app at the existing `fixtures/pgn/` corpus (built for `cargo test`, not discovered as manual test material). Full writeup in `docs/backlog-archive.md`. |
| B-077 | chore | P2 | proposed | Performance criteria must be median/p95 plus a control, never an absolute max | Lesson from B-048: the agreed "max frame < 16.7 ms" threshold was unattainable by construction at 60 Hz vsync, and macOS Low Power Mode silently capped the machine to 30 fps, nearly producing a false negative against ADR-0001. A plain-browser control on the same machine is what caught it. Record power state and Low Power Mode with any benchmark |

## Rejected / deferred (kept on purpose)

| ID | Type | Status | Description | Why |
|-------|---------|----------|-------------|-----|
| B-034 | idea | rejected | Build our own chess engine | Explicit non-goal. This is a GUI; engines are a solved, specialised problem |
| B-035 | idea | rejected | Become an all-in-one online chess platform (play vs. humans, lobbies, ratings) | Explicit non-goal in the vision. Chess.com/Lichess own this |
| B-036 | idea | deferred | Cloud sync / hosted account for the database | Conflicts with local-first data-ownership stance; revisit only if multi-device demand is real. Would trigger auth + infra gates |
| B-037 | idea | deferred | Mobile / tablet clients | Cross-platform means Windows, macOS, Linux desktop for now |
| B-038 | idea | deferred | Web version of the app | Same reason; also complicates local engine execution |
| B-039 | idea | deferred | Social features: sharing, comments, public collections | Out of scope; the product is a personal library |
