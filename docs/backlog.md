# Backlog

> The system of record for **all** work: features, bugs, technical debt, and rejected ideas.
> Append-only — nothing is deleted. Items change status; they don't disappear.

## Conventions

- **ID:** sequential, never reused (e.g. `B-001`).
- **Type:** `feature` · `bug` · `tech-debt` · `chore` · `idea`
- **Priority:** `P0` (now) · `P1` (soon) · `P2` (later) · `P3` (someday)
- **Status:** `proposed` · `approved` · `in-progress` · `done` · `deferred` · `rejected`

Deferred ideas worth keeping but not scheduling can also live in `docs/parking-lot.md`.

## Items

| ID | Type | Priority | Status | Description | Notes |
|-------|---------|----------|-------------|-------------|-------|
| B-001 | chore | P0 | done | Initialize repo with AI-dev starter kit | Methodology, prompts, handover, backlog in place |
| B-002 | chore | P0 | done | Stage 0 — one-page product vision | Approved and committed as `docs/product-vision.md`. Gate passed |
| B-003 | chore | P0 | done | Decision Gate: UI framework / desktop shell | **Tauri 2** + TypeScript frontend. See ADR-0001. Frontend stays Electron-portable via a single IPC adapter module |
| B-004 | chore | P0 | done | Decision Gate: local storage / database engine | **SQLite. See ADR-0004.** Also **downgraded from hard stop to notify-and-proceed**: PGN files are retained as source of truth and the MVP is read-only (B-050), so the DB holds nothing that exists only there — abandoning it costs a re-import, not data. Gate re-hardens at **B-015** (annotations), when that stops being true. Conditions: DB stays derivable, import idempotent, verbatim PGN in the row, stable row IDs from the first migration. Schema questions B-058 – B-060 are *not* downgraded — decide at B-054 |
| B-005 | chore | P1 | done | Decision Gate: chess rules/PGN library vs. own implementation | **Buy jobs 1–2, build job 3.** Rust `pgn-reader` + `shakmaty` for import; TS `chessops` for the open game; `chessground` for the board; no third-party Rust game tree. See ADR-0003 |
| B-006 | chore | P1 | proposed | Decision Gate: engine process management & UCI transport | Hard stop if it becomes a platform-surface commitment (bundled engine, sandboxing, code signing) |
| B-007 | feature | P0 | done | PGN import (file + paste) into local database | **DONE — all four milestones (session 8).** Milestone 4 delivered file import: `src-tauri/src/files.rs` (IO but no Tauri, so it is compiled and tested rather than trusted), the `import_pgn_files` command, `src/shell/files.ts` for the native picker and the window drag-drop event, the dialog's Files tab, and the per-file report. **The finding invalidated a shape rather than a fact: "at most one failure per import" is a property of one *input*, and several files are several inputs**, so an operation can now report a failure followed by later successes — the "n games with holes" case the standing constraint warns against, arriving legitimately because the holes fall between files. `ImportReport` grew a per-file dimension and deliberately leaves `failure` null for a multi-file report, because synthesising a "first failure" would let every milestone-3 consumer keep compiling while quietly hiding the rest. **Two owner decisions taken against the surveyed default:** a drop opens the dialog on its Files tab listing what was caught and waits for Import — no product in the category stages files, but a *drag* is easy to make by accident where an OS picker is not, and naming the files is the confirmation; and **one game imported offers to open it**, for drop and paste alike, which is an instance of the standing rule (*the strip records; the dialog stops you when there is a decision*) rather than an exception to it. Lichess does the opposite and is noted in the survey. **Three faults found by looking at rendered output rather than by a green build:** every number in the catalogue was interpolated bare, so a real byte offset rendered `2180442` — i18next's `Intl` formatter had been available and unused since milestone 3; an unreadable file said "0 games" beside "could not be opened", which reads as a measurement; and per-file failure lines were flush with the file name. **Unverified as always:** the Tauri command signature, the picker and the drop event need a window. Earlier: **Milestone 3 of 4 done (session 7): IPC and the paste path — and the first part of this project a human has used rather than read.** `import_pgn_text` with one long-lived `Importer` in Tauri state; the **Add games dialog**; `ImportStrip`; `ImportOutcome`; `importReport.ts` (pure, tested); `src/mock/games.ts` deleted. **The UI was mocked and approved before it was coded**, and the survey behind it is in `docs/ui-survey.md` — with two findings that would have been guessed wrong: **two of the four surveyed products have no paste box at all** (ChessBase watches the clipboard, En Croissant is file-and-account only), and the newest organises import **by source, as tabs**, which is the shape B-012/B-013/milestone 4 already need. **Three owner findings while running it, all resolved in the session.** A clean import left the dialog open — awkward; closing it outright then lost the record — so the settled rule is **the strip always records, the dialog additionally stops you only when there is a decision**, which is intrusiveness matched to criticality rather than inconsistency. And the info panel formatted real chess.com data poorly, rebuilt as curated groups over a full-tag disclosure (see B-105). **Deliberate omissions:** drag-and-drop waits for milestone 4 because a dropped file is Tauri's file-drop event, a platform surface (B-069); a pasted string is **not** sniffed for PGN-ness, because deciding what a valid game looks like is the validation ADR-0009 declines. **Still unmeasured: how long 3,000 games takes** (B-033) — the owner's paste was a month of games, not a decade. **Next: milestone 4**, file import. Earlier: **Milestone 2 of 4 done (session 6): the pure import module.** `src-tauri/src/import/` — `mod.rs`, `model.rs`, `decode.rs`, `derive.rs`, `visitor.rs` — plus `tests/import_corpus.rs`, which turns `expected.json` from a record into a guard. 32 Rust tests, fmt and clippy clean, **and verified by the AI rather than handed over unverified**, because this container turns out to have a Rust toolchain and crates.io access: all eighteen fixtures reproduce session 5's macOS numbers exactly, which is the control that makes the rest trustworthy. **The finding: `pgn-reader`'s errors are terminal, and that falsified an acceptance criterion.** Its whole error vocabulary is two messages (`unterminated tag`, `unterminated comment`, read from the crate source), and an unterminated `{` swallows the rest of the input — so there is at most one error per input and every game after it is lost, where the spec promised "3,000 games with 4 bad ones imports 2,996". **Owner's call: no resynchronisation** — report loudly with the byte offset and the failing game's roster tags, fix the criterion, pin the behaviour in a test; adding recovery later is fifteen lines, removing it once shipped is not. Also measured: the `Result` tag and the termination marker are **both reported and never reconciled** (tag `1-0`, marker `0-1`, same game), so **the tag wins** per ADR-0005 and the marker is the fallback — which means we deliberately disagree with chessops on that one file. **And one real bug caught by a test:** stripping accents by Unicode decomposition also decomposes Cyrillic `й`, rewriting `Анатолий` as `анатолии` — a different player. Marks are now dropped only on Latin bases.  Earlier: **Milestone 1 of 4 done (session 5): the measurement.** `src-tauri/tests/pgn_reader_probe.rs` ran `pgn-reader` over all eighteen fixtures and the results are in `expected.json` (`importOutcome`/`importTags`/`importTokens`). **It refuses exactly one of eighteen** — `unterminated-comment` — so the import-error set is nearly empty, which ADR-0009 suspected and could not claim. **Headline finding: the two libraries produce two different wrong games from the same non-English file, silently** — `pgn-reader` drops unparseable tokens, `chessops` rewrites them; hence the English-SAN assumption and known issue **B-115**. **Three of my documented "accepted risks" were chessops limitations rather than general ones**: pgn-reader reports tags truthfully (2 where 2 exist, 9 with duplicates, 0 for garbage), so one risk-table row is retired and duplicate tags become a *choice* the importer makes rather than a check. **That "next" is now done — see the milestone-2 note at the head of this cell.** Spec: `docs/feature-specs/b007-pgn-import.md`, still awaiting formal approval. Earlier note: **Spec drafted session 5.** Large tier. Two scope calls shape it. **Paste before file**, because a file dialog is a platform surface (B-069) and paste needs none — so the smallest demonstrable slice is pasting an export and watching real games fill the library table. **No database**: import produces `Game` values held in memory for the process lifetime, and B-011 persists them, which keeps SQLite, the migration and player-identity resolution out of a feature that is already large. Six milestones, each ending in a command the owner runs. **Milestone 1 is a measurement with no product code**, and it is the important one: `pgn-reader`'s tokenizer behaviour is unknown, and B-099 measured that `chessops` silently rewrites `Sf3` to `f3` rather than rejecting it, so whether the Rust side agrees decides how rule 4 is implemented — and a disagreement is a genuine **B-064 finding, tested at last rather than discussed**. The corpus is what makes the Rust half verifiable despite the sandbox having no cargo: one `cargo test` asserts eighteen cases whose expected values were measured before the code existed. Also settled in the spec: `shakmaty` needs `features = ["variant"]` (opt-in, a compile error if omitted); the warning vocabulary stops being provisional at milestone 3; **rule 5 is unreachable until file import**, since pasted text has already been decoded by the webview; and re-importing the same text twice must produce duplicate rows, with a test to stop anyone "fixing" it into a silent merge. Original note: MVP core. Must survive messy real-world PGN |
| B-008 | feature | P0 | proposed | Game list view with sort and column selection | MVP core |
| B-009 | feature | P0 | proposed | Board + move list; play through a game, navigate variations | MVP core |
| B-010 | feature | P0 | proposed | Header search & filter (player, event, date, result, ECO) | MVP core |
| B-011 | feature | P0 | done | Local persistence of the database across app restarts | **All four milestones done (session 9).** Committed `297f3e0` (spec/ADR addendum), `f03b584` (M1), `3e47802` (M2+M3), `1def042` (docs), pushed to `origin/main`. `docs/feature-specs/b011-persistence.md` has the full plan and acceptance checklist, now fully checked. M1 (`src-tauri/src/db.rs`, the SQLite layer) compiled and tested in a mirror crate — 10 new tests, `cargo fmt`/clippy clean. M3 (the frontend: `GameSummary` split from `Game`, `listGames`/`getGame`, `App.tsx` re-reading the library instead of holding imported games) verified against the real toolchain — typecheck, 86 tests, i18n check, build, plus a headless screenshot pass. **M2 re-verified on the owner's machine**: `cargo build`/`cargo test`/`cargo clippy` all green against the real Tauri crate, 48 Rust tests, `rusqlite` fetched and compiled for the first time with no issues. **One documented policy changed**: `import_pgn_text`/`import_pgn_files` now return `Result<_, AppError>` — parsing still cannot fail (ADR-0009 unchanged), but persisting to the database now can. **M4 owner-run and confirmed**: imported games, quit, relaunched, games were still in the library. See `docs/handover.md`'s "Session 9" section for the full account, including a recurring `.git/HEAD.lock` permission issue on this session's file bridge — resolved each time by the owner, not a repo problem. MVP core |
| B-012 | feature | P1 | proposed | Chess.com import (account/archive) | Post-MVP. **Shape decided in B-102:** walk `/pub/player/{u}/games/archives`, fetch each month's **JSON** (not `/pgn`), unwrap it, and feed each game's `pgn` field through B-007's pipeline; take `uuid`, `rules` and `accuracies` from the JSON. Dedupe on `uuid`. Practical notes from the real download: **a mandatory descriptive User-Agent** including a contact address, **serial requests** because parallel ones get 429'd, and **empty months are normal** — 7 of 20 archives returned `{"games":[]}`. There is no single-game endpoint; `getGameById` in the popular npm wrapper is not part of the official API. Original note: | API rate limits and pagination |
| B-013 | feature | P1 | proposed | Lichess import (account/study/PGN export) | Post-MVP |
| B-014 | feature | P1 | proposed | UCI engine integration — attach engine, analyse position, show PV/eval | First post-MVP milestone |
| B-015 | feature | P1 | proposed | Annotations: comments, NAGs, variations, editing a game | Requires PGN write path |
| B-016 | feature | P1 | proposed | Create games manually (enter moves, set up position/FEN) | |
| B-017 | feature | P1 | proposed | PGN export (single game, selection, whole database) | Data-ownership commitment; pairs with import |
| B-018 | feature | P1 | proposed | Position search (find games reaching a position / pattern) | Expensive; needs position index (B-042). Design before building |
| B-019 | feature | P2 | proposed | Full-game engine analysis pass (blunder detection, accuracy) | |
| B-020 | feature | P2 | proposed | Collection-level analysis (opening repertoire, results by ECO, trends) | From vision "over time" list |
| B-021 | feature | P2 | proposed | Advanced search: boolean/saved queries, material & endgame filters | |
| B-022 | feature | P2 | proposed | Game management: tags, collections/folders, merge, dedupe, batch edit | **Dedupe landed here by decision, not by omission (ADR-0008 addendum, B-113).** Rule 6's content hash was removed from the MVP because derivability needs only drop-and-rebuild, so nothing in B-007 deduplicates and importing one file twice produces duplicate rows — visible and removable, which is the failure mode the ADR preferred over a silent false merge. Whatever is built here inherits three things already worked out: a key over the *normalised* game rather than the bytes (`fixtures/pgn/dedupe-*.pgn` are the specification, and demonstrate a re-export that must still match), **the variant must be part of any identity** since the Seven Tag Roster does not carry it, and API-sourced games key on `uuid` instead (B-102). `scripts/survey-pgn.mjs` already computes and reports collisions, so the evidence exists before the feature does |
| B-023 | feature | P2 | proposed | Opening explorer / tree view over the user's own games | |
| B-024 | feature | P2 | proposed | Design system: tokens, dark/light themes, board & piece theming | Vision's "modern" claim depends on this being deliberate |
| B-025 | feature | P3 | proposed | Play against an engine | Explicit "over time" item; not a play platform |
| B-026 | feature | P3 | proposed | Fair-play analysis | Explicit "over time" item. Ethically sensitive — needs its own spec |
| B-027 | feature | P3 | proposed | Endgame tablebase lookup (local or online) | |
| B-028 | feature | P3 | proposed | Opening-book (.bin/Polyglot) support | |
| B-029 | feature | P3 | proposed | Import from ChessBase formats (.cbh/.cbv) | Proprietary, poorly documented — high effort |
| B-030 | feature | P3 | proposed | Training / spaced-repetition on own mistakes | Adjacent product; keep out of core |
| B-031 | chore | P2 | proposed | Open-source readiness: licence, CONTRIBUTING, issue templates, public repo hygiene | Stated intent is to open source for the chess community |
| B-032 | chore | P2 | proposed | Packaging & distribution per platform (installer, signing, notarisation, auto-update) | Cost and friction often underestimated |
| B-033 | tech-debt | P2 | in-progress | Import performance target: define & measure (games/sec, DB size ceiling) | **The import half is measured (session 8) and the risk was in the wrong place.** Through the real `Importer`, at the shipped `opt-level = "s"`, on two vCPUs of the AI container: **3,000 games ≈ 95 ms end to end** — 40 ms decode+parse, 23 ms serialise to JSON, 32 ms `JSON.parse` — and **10,000 games ≈ 340 ms**. The owner's machine will be faster. **Parsing is not the cost; moving the result is.** The IPC payload is **1.5× the source file** (10.2 MiB for a 7.0 MiB export; 33.9 MiB for 23.3 MiB) because ADR-0005 puts the verbatim PGN in every row — and **the library table reads none of it**, only the game you open. That is B-011's to fix by keeping the PGN in the database and fetching per game, and it is recorded rather than acted on here. Two consequences taken immediately: **B-067 progress streaming is not justified** for import, and there was no measured reason to import files one at a time. Method: a mirror crate with a *copy* of the real manifest, a synthetic chess.com-shaped corpus (21 tags, ~40 moves, `%clk` on 88%), and the whole Rust suite re-run in the same crate as a control that the mirror is faithful. The `JSON.parse` figure is V8 and the macOS webview is JavaScriptCore, so treat it as an order of magnitude rather than the answer. **Still open, and it is the half that matters now:** rendering 10,000 rows, and the <200 ms filtered-search target, both of which arrive with B-008/B-010 and TanStack. Original note: Turns "fast" from adjective into number. MVP target: 10k games, filtered search <200 ms |
| B-040 | feature | P2 | proposed | Reference database: millions of master games, read-mostly, bulk-loaded | Post-MVP. Separate store from the personal DB — different shape, different access pattern. Constrains B-004 by requiring it not be designed out |
| B-041 | feature | P2 | proposed | Opening explorer over reference DB: move frequencies + W/D/L statistics | The reason B-040 exists. Depends on B-040 and B-042 |
| B-042 | tech-debt | P2 | proposed | Position index design (Zobrist-style hashing, dedupe, storage cost) | Shared foundation for B-018 and B-041. Spec before building either |
| B-043 | chore | P0 | proposed | Source data for reference DB: find a masters game set with redistributable licensing | Blocks B-040. Licensing and download size are the real constraints, not the code |
| B-044 | chore | P0 | done | git init, secrets hygiene, per-repo pseudonymous commit identity, push to GitHub | Initial commit `99a6079` pushed to `origin/main`. Identity verified pseudonymous; `.DS_Store` excluded |
| B-045 | chore | P2 | proposed | GitHub repo hygiene: branch protection, issue/PR templates, CODEOWNERS, Dependabot | Do when the repo goes public, not before |
| B-046 | chore | P1 | done | CI on GitHub Actions: build + test matrix across Windows, macOS, Linux | **Green on all three platforms**, plus the frontend guardrails and `cargo fmt`/clippy. Took three runs. **It paid for itself immediately:** run one failed on Windows alone with a missing `icons/icon.ico`, which macOS and Linux do not need and no developer machine could have surfaced. That is precisely the divergence this item exists to catch, found on day one instead of by the first Windows tester (B-070) a year from now. `fail-fast: false` is what made it obvious the break was platform-specific rather than universal. **Promoted from P2 by B-068.** | Now the primary guardrail: shipping macOS-first is only reversible if the other two keep *compiling*. GitHub's Windows and Linux runners are free, so build-level divergence gets caught at zero cost even though interactive testing is deferred |
| B-047 | chore | P3 | proposed | Distribute releases via GitHub Releases (installers per platform) | Distribution channel decision is separate from repo hosting; revisit at B-032 |
| B-048 | chore | P0 | done | Spike: board render + move interaction while a UCI engine floods stdout | **Passed — ADR-0001 stands.** Frame time median/p95 17.0 ms (vsync) *identical* engine-stopped vs engine-running; worst frame was lower under load. Events pinned at 10.0/sec, zero drops. Findings in `docs/tech-stack.md`. **Main finding is that the premise was wrong:** Stockfish bursts (~44 lines/sec peak, MultiPV 8) then goes near-silent at depth — no sustained flood. Spike code discarded. Linux half remains B-066 |
| B-066 | chore | P2 | deferred | Repeat the B-048 spike on Linux / WebKitGTK | The actual close-out of handover risk 1. Deferred by B-068 (macOS-first). Until it runs, ADR-0001's Linux assumption is untested, not validated |
| B-068 | chore | P0 | proposed | Release sequencing: all three platforms remain the target; release order gated by testing capacity | **Not a scope reduction.** Cross-platform stays a first-class goal per the vision. The constraint is *verification*, not architecture: only macOS can currently be tested, so macOS releases first. Windows and Linux release as testers become available. All three build in CI from day one (B-046) |
| B-069 | tech-debt | P0 | in-progress | Portability guardrails so the untested platforms stay shippable | `src/shell/platform.ts` created at B-054 with platform detection, accel-key (Cmd vs Ctrl), path separator, and native line ending behind one boundary. Detection is user-agent based for now — crude, but swappable for `@tauri-apps/plugin-os` in one file, which is the point. **File dialogs closed at B-007 milestone 4 (session 8):** `src/shell/files.ts` owns the native picker and the window drag-drop event, behind `tauri-plugin-dialog`, and `src/shell/opener.ts` owns external URLs. **The guardrail that protects this boundary had two holes, both found by using it rather than reading it** — it matched `@tauri-apps/api` but not `@tauri-apps/plugin-*`, and it inspected only *static* imports while `ipc.ts` has used a dynamic one since milestone 3. Both now fire, verified with a negative control. **Still open:** menu-bar conventions, engine binary discovery. Original note: | Leak points: path handling, file dialogs, Cmd vs Ctrl shortcuts, menu-bar conventions, engine binary discovery, line endings. Keep behind abstractions from the start. Cheap now; expensive after a year of implicit macOS assumptions |
| B-070 | chore | P1 | proposed | Recruit Windows and Linux testers | **The actual dependency behind B-068, and previously unrecorded anywhere.** Nothing else unblocks Windows/Linux release. Natural source is the chess community the project already intends to open-source to (B-031) — which gives public-repo timing a functional reason to happen earlier, not just an ideological one |
| B-071 | chore | P1 | proposed | Headless smoke tests on Windows/Linux in CI: app launches, DB opens, PGN imports | Reduces the tester dependency without removing it. Catches the crashes and missing-runtime failures that dominate first-run problems on an untested platform; cannot catch anything about how it looks or feels |
| B-067 | tech-debt | P1 | proposed | Throttle high-frequency backend→frontend streams in Rust, not the frontend | **Validated by B-048 and written up in `docs/tech-stack.md`.** Parse in Rust, keep latest state (overwrite, never append — flat memory by construction), emit on a fixed timer. Measured 10.0 events/sec with zero drops while source rate varied 0–44 lines/sec. Kept at P1 despite the flood being smaller than feared: it costs almost nothing and removes a class of failure. **Applies to engine output only — the import half is settled and the answer is no (B-033, session 8):** 3,000 games is ~95 ms end to end and 10,000 is ~340 ms, so a progress bar would flash and vanish, which is worse than none. If import ever needs it, the trigger is B-011's database write rather than the parser. Carry into `docs/architecture.md` when it exists |
| B-049 | chore | P1 | done | Decide PGN import fidelity policy: accept / repair / reject malformed input — **superseded by B-114 / ADR-0009** | **Read B-114 first: ADR-0008 was superseded in session 5.** The owner judged the tiered, permissive policy below disproportionate, and ADR-0009 replaced its seven rules with four — import or error, no repair. What survives from this item is the part that was right and cheap: **the per-game unit, so a file is never the unit of failure**, verbatim PGN as the source of truth for imported games, and variant selection. The rest is kept as the record of how the question was worked through, including the two challenges that changed it. Original note: **Decided as ADR-0008 (proposed, awaiting owner acceptance).** Vision open question 2 answered. **The framing turned out to be the trap:** accept/repair/reject sounds like three dispositions for a file, but ADR-0005 already stores verbatim PGN in the row, so repair cannot corrupt and rejection cannot lose — what is actually being decided is how much *derived* understanding a partly-comprehensible game gets, and how the shortfall reaches the user. Answer: **tiered per game, never per file** — `clean` / `imported`-with-warnings / `quarantined`-but-retained, with the disposition itself derived so a better parser upgrades old rows on re-import. Seven rules; the four that were hidden sub-decisions are legality validated but never blocking (agreeing with `mainline.ts`, which already truncates), **notation language decided per file and never per token** (see B-098 — `R` is rook in English and *roi* in French, so legality cannot arbitrate), UTF-8 with Latin-1 fallback, and duplicate identity as a content hash over the normalised game rather than the bytes. Raised B-097 – B-101. Adds a derived disposition field to the game model, which B-011's migration must carry. **Amended within the session after the owner challenged the premise** — the claim that warnings would be common was unevidenced and is withdrawn (B-101 measures it properly, B-098 dropped to P3), and following the challenge exposed a real gap: variant games arrive inside *clean* exports and would have been mislabelled as illegal moves (rule 3b, B-100) | Notify-and-proceed tier, so recorded and proceeding. **The highest-regret rule is the duplicate key** (rule 6): it asserts that identical players, event, date, round, result and moves mean the same game, which is almost always true and is not a theorem. Accepted because a false merge is visible and recoverable while accumulating duplicates on every re-import is neither |
| B-050 | chore | P1 | done | Decide whether MVP is read-only or writes annotations | **MVP is read-only.** Its job is to validate the import path and the look and feel; neither needs writes. Vision §4 and open question 1 amended. **Condition attached:** read-only is a build-order choice, not a schema shape — stable row IDs, verbatim PGN as source of truth, header columns strictly derived, so B-015 can be added without migration. Simplifies the B-004 gate: no edit/drift path to design for at MVP |
| B-051 | chore | P1 | proposed | Decide whether to bundle an engine (e.g. Stockfish) or require user-supplied | Vision open question 3. Distinct from B-006: that is transport, this is licensing, binary size, signing, per-platform builds |
| B-052 | chore | P2 | done | Decide open-source timing and licence | **GPL-3.0-or-later.** Forced by the chess stack (shakmaty/pgn-reader/chessops/chessground all GPL-3.0+), and aligned with stated intent. See ADR-0002. Public-repo *timing* still open — tracked under B-031 |
| B-057 | chore | P0 | done | Add `COPYING` — full, verbatim GPL-3.0 text | Fetched from the FSF and verified: 674 lines, 35,149 bytes, correct header and closing paragraph. In place before any GPL dependency was added |
| B-065 | chore | P1 | done | Add SPDX identifiers (`GPL-3.0-or-later`) to `Cargo.toml` and `package.json` | Present in both manifests from the commit that created them, so no window existed where the repo carried code without a declared licence. `tauri.conf.json` also points `bundle.licenseFile` at `COPYING` |
| B-058 | tech-debt | P1 | done | Player identity: separate `players` table with FK, plus name normalisation | **Decided in ADR-0005**, expressed as `Player` in `src/model/game.ts`. `normalisedName` is derived and lossy; the verbatim name stays authoritative. The migration itself is B-011; identity merge is B-022. Original note: | Case, accents, `Lastname, Firstname` vs `Firstname Lastname`. Needed for "my Sicilians as Black" (B-010) and for dedupe/merge (B-022). More work up front, much less pain later |
| B-059 | tech-debt | P1 | done | Partial-date handling: store raw PGN date string *and* a nullable parsed date | **Decided in ADR-0005** as `PgnDate` — `raw` authoritative, plus derived `parsed`/`year`/`month` so year-only dates still sort. Formatting goes through `src/i18n/format.ts` using `Intl`, never string concatenation. Original note: | Real PGN routinely carries `2024.??.??` or `????.??.??`. Sorting/filtering needs the parsed value; fidelity needs the original |
| B-060 | tech-debt | P1 | done | Store the full PGN tag set as JSON alongside extracted columns | **Decided in ADR-0005.** Hot fields promoted to indexed columns and strictly derived; `tags` retains everything; `result` is an integer (`1`/`0`/`-1`/`null`), never `"1-0"`. Original note: | Real files carry `Annotator`, `PlyCount`, `Variant`, `FEN`, `SetUp`, `WhiteTitle`, and site-specific tags. Extract the hot ones; keep everything so nothing is silently dropped. Store `Result` as an integer, not `"1-0"` |
| B-061 | idea | P3 | proposed | zstd-compress stored PGN text | Raw PGN compresses extremely well. Post-MVP; noted so it isn't rediscovered |
| B-062 | idea | P3 | proposed | SQLite FTS5 over player and event names for fuzzy search | Cheap once the store is chosen. Post-MVP |
| B-063 | tech-debt | P2 | proposed | Annual dependency upgrade chore: `pgn-reader` / `shakmaty` 0.x breaking releases | pgn-reader's author describes maintenance as minimal, following shakmaty. Pin versions; expect one upgrade pass per year |
| B-064 | tech-debt | P3 | deferred | Keep the two chess rule implementations consistent (shakmaty vs. chessops) | **Deferred P2 → P3 (session 5): for the MVP there is nothing to keep consistent.** This risk exists only because both libraries walk moves — and under ADR-0009 the importer does not. `pgn-reader` validates syntax; legality is checked on the display side by `chessops` alone, for the one game the user opens. **One walker, no divergence.** It returns when Rust genuinely needs positions: the position index (B-018/B-042) and engine analysis (B-019), both post-MVP — and it returns *smaller*, because `fixtures/pgn/` already exists to test it against, which is the shared corpus this item always wanted. **One live comparison remains worth making at B-007 milestone 1**, and it is about parsing rather than rules: `chessops` silently rewrites `Sf3` to `f3` and keeps the first of two duplicate tags. If `pgn-reader` differs, the same file yields different data on the two sides — a real finding, on files neither library considers broken. Original note: Accepted cost of the ADR-0003 split. They never arbitrate the same question — Rust at import, TS at interaction — but divergence would be confusing. Consider a shared test corpus |
| B-053 | chore | P0 | done | Stage 1 deliverable: core workflows document | **Written as `docs/core-workflows.md`**, ahead of the layout choice rather than after it. Six journeys (W1–W6). Central finding: the unit of work is not "one game" but *a question several games answer*, and three workflows make conflicting layout demands — W2/W5 want the list prominent, W3 wants the board dominant, W4 needs several games open at once. **It immediately paid for itself:** testing layout C against W2 exposed a scan → open → reject → next loop that tabs make expensive. Two fixes were tried in the library's side panel — a preview board, then a details panel — and both were removed; the preview showed the same starting position for every row, and the details duplicated the table's own columns while charging a fifth of the width. **The answer was that W2 needs nothing added**: the columns that identify a half-remembered game are already in the table, so the library tab is now full-width with no panel. Original note: | The one Stage 1 deliverable the vision does not already cover. 4–6 end-to-end user journeys (import → find → open → play through). Feeds the skeleton's screen list. **Promoted in practice by B-084:** it was skipped as "not on the critical path" and the skeleton immediately got its screen model wrong. Do it before B-007/B-008 rather than discovering the same thing again at a larger scale |
| B-054 | chore | P0 | done | Milestone M1 — Skeleton: window, board, game list, navigation | **Done and now fully verified — the app runs on macOS with no errors, and the secondary paths were walked by the owner in session 4.** Library table, tabs opening and closing, arrow-key move navigation with the move list following, window resize, Info tab, filter box, Enter-to-open, click-a-move, tab-switch position retention, footer move buttons, no document scrollbars at any window size. Home/End confirmed via fn+←/fn+→. **All eight secondary paths pass, and all three findings came from paths that passed** (M1-F1 – M1-F3 in `docs/milestones/m1-skeleton.md`): no key on the development keyboard is labelled `Home` or `End`, so the working binding is undiscoverable → widened B-086; Accel+W on the library tab falls through and closes the window, examined and **kept** as the macOS convention, with the early return in `App.tsx` now commented so it survives → M1-F2; and the live-resize stretch was chased with a control and turned out to be the webview's, not ours → B-096 rejected. The general point is the one the project keeps relearning: **every box was expected to pass, every box did pass, and walking them still returned three things — a tick is not the same as an absence of information.** **The layout took three attempts** and the third was chosen from a survey, a workflows document and mocked options rather than guessed; see ADR-0007 and B-087. Earlier note: **First product code exists.** `docs/milestones/m1-skeleton.md` written; Tauri 2 + React 19 + Vite scaffolded; board, game list and navigation built; guardrails B-069/B-072 and CI B-046 landed with it. Frontend verified green (`tsc --noEmit`, `vite build`, `check:i18n`). **Not done:** `cargo build` and the window opening are unverified — the AI sandbox has no Rust toolchain, so that half must be run in a real terminal |
| B-055 | chore | P1 | proposed | Create `docs/tech-stack.md` and `docs/architecture.md` | Stage 2. Written immediately after B-003/B-004 land, not before — they have nothing to say until then |
| B-056 | idea | P3 | proposed | Position setup by FEN paste as a cheap early board test | Falls out of B-016; useful as a skeleton-stage smoke test before any database exists |
| B-072 | tech-debt | P1 | in-progress | i18n foundation: externalise every user-facing string from commit one | Landed at B-054: `i18next` + `react-i18next`, typed keys (a misspelt key is a compile error), `Intl`-based date and collation helpers in `src/i18n/format.ts`, and `npm run check:i18n` enforcing guardrails (1) and (2) in CI via the TypeScript parser. Rust side returns `AppError { code, detail }` — no English prose crosses IPC. **A fourth gap closed at session 8, and it had been open since milestone 3: no number in the catalogue was formatted.** Counts and byte offsets were interpolated bare, so a real 7 MiB export reported `Nothing after byte 2180442` — correct in no locale, and a space or a full stop in several. i18next's `Intl.NumberFormat` formatter was available the whole time and nothing used it; every user-visible number is now `{{value, number}}`. **Found by looking at a rendered report, not by a typecheck or a test** — the same door as the milestone-3 plural bug, one step further along. **Guardrail (3), the pseudo-locale build, is not done — split out as B-081.** Original note: | **English is the only locale in MVP; the point is that adding a second is a translation job, not a refactor.** Three guardrails: (1) no user-facing string literals in components — everything through a message catalogue with ICU plural/gender support; (2) the Rust backend returns *error codes*, never English prose — the frontend owns all wording; (3) a pseudo-locale build in CI proves no string escaped and no layout clips. Same economics as B-069: near-free now, a year-long grind later. Sits alongside B-024 (design system) since layout must tolerate ~35% text expansion for German |
| B-073 | tech-debt | P2 | proposed | Separate storage notation from display notation (localised SAN + figurine) | **The chess-specific half of i18n, and the part that would be missed.** SAN piece letters are language-dependent — `Nf3` / `Sf3` (de) / `Cf3` (fr). PGN mandates English SAN on disk, but real exports from German software do not always comply, so import must *tolerate* localised SAN (B-049 fidelity policy) while storage stays canonical English. Display then renders per-locale, with figurine notation as the escape hatch that sidesteps language entirely. Getting this wrong means notation strings leak into the schema |
| B-074 | tech-debt | P2 | proposed | Locale-aware collation, dates, and number formatting | Player-name sorting must use locale collation, not byte order — accents and non-Latin scripts sort wrongly otherwise. Ties directly to B-058 (name normalisation) and B-059 (partial dates): store canonical, format at the edge. Never hand-roll date strings |
| B-075 | chore | P3 | proposed | Decide which locales ship after English | Not urgent — B-072 makes it cheap whenever it happens. Natural first candidates follow the chess world rather than general market size: German, Spanish, Russian, French |
| B-076 | tech-debt | P2 | proposed | Re-test engine output rate under the *realistic* pattern: rapid position changes | B-048 ran one long analysis, which is the quiet case — Stockfish only floods during the shallow-depth phase. A real GUI restarts the search on every move, so clicking quickly through a game replays the burst continuously. That is the untested case, and it is the normal one. Cheap to fold into B-014 |
| B-078 | tech-debt | P3 | deferred | Keep the database derivable: rebuildable from PGN, source of truth retained | **Downgraded P1 → P3 and deferred (session 5), on the owner's objection that this is a production-level concern being carried by a development-stage app.** The objection is right, and the cost of it was real: derivability had accreted an owner, a gate that "re-hardens", conditions quoted in two ADRs, and a place in three separate documents — **for a fact that fits in one sentence. We do not delete the PGN files, so the database can be rebuilt by importing again.** Nothing enforces that, nothing needs to, and there is no user data to lose. Drop the database and re-import: that is the whole recovery procedure, and it is available today without anyone building it. **What was genuinely wrong with the earlier framing:** it treated "the DB might be abandoned" as a data-safety problem, which it is not while every game came from a file that is still there. That inflated ADR-0004's reasoning, inflated ADR-0008's, and made a "rebuild database from source PGNs" command look like a requirement rather than a convenience. **When this becomes real, and it does:** B-015 introduces annotations, which are the first data that exists nowhere else, and the concern arrives with actual users who could lose something. At that point an export path (B-017/B-079) is the answer, not a derivability doctrine. Until then this item should not be scheduled and should not be quoted as a constraint. Original note: | **Wording narrowed by ADR-0008's session-5 addendum (B-113), and the distinction is the substance of this item.** It previously said "re-importing the same PGN must not duplicate games", which conflated two different properties. **Derivability is: dropping the database and rebuilding it from the retained PGN reproduces the same database** — that holds with or without dedupe, because each source file is read once per rebuild, and it is the only part ADR-0004's gate downgrade actually rests on (nothing exists solely in the database). **Dedupe is the separate property of importing the same file twice into an existing database**, which the MVP no longer attempts — rule 6 is out, duplicate rows are permitted, visible and removable, and merging is B-022's subject. So the tested path this item owns is **drop and rebuild**, and a "rebuild database from source PGNs" command is still the cheapest way to keep it honest. Original note: **This is the condition ADR-0004 rests on, so it needs an owner rather than good intentions.** Re-importing the same PGN must not duplicate games; dropping the DB and rebuilding must be a supported, tested path. The moment this quietly stops being true, the storage gate re-hardens and nobody notices. A "rebuild database from source PGNs" command is the cheapest way to keep it honest |
| B-079 | chore | P1 | proposed | Build PGN export (B-017) alongside annotation (B-015), not after | Falls out of ADR-0004: annotations are the first data that exists only in the database, which is what re-hardens the storage gate. Export defuses it again by making the DB derivable-in-reverse. Shipping annotation without export creates a window where the user's only copy is in a schema we reserved the right to change |
| B-095 | chore | P2 | proposed | Replace the placeholder icons with real artwork, and generate the full set | Current icons are a generated checkerboard: `icon.png` (512) and `icon.ico` (BMP 16–128 plus PNG 256). They exist to make the build work, not to look like anything. Real bundling (B-032) additionally wants `icon.icns` for macOS and the assorted PNG sizes Windows Store and Linux desktop files expect — `tauri icon` generates the whole set from one source image. Pairs with B-024, since the app icon is part of the design system rather than a build artefact |
| B-094 | tech-debt | P2 | done | Verify the MSRV claim, or stop making it | **Closed session 6, and the answer was that the claim was already false — no compiling required to find out.** The dependency graph sets the floor: `pgn-reader 0.29.0` declares `rust-version = "1.88"` and the `shakmaty 0.30.1` it pulls in declares `1.95`, both read from their own manifests. So `1.77.2` had been untrue since session 5 added the crate, and a build on 1.77.2 would have failed at resolution rather than on our source. **Corrected to `1.95`**, which is what CI has pinned all along, so the declaration now matches the only toolchain anything is tested on. The AI sandbox can reach crates.io but not `static.rust-lang.org`, so installing an old toolchain to confirm by compiling was not possible — but it was also not necessary, because a declared MSRV below a dependency's is wrong on its face. **The transferable bit: the cheapest check was reading the dependencies' manifests, and nobody had.** Original note: `src-tauri/Cargo.toml` declares `rust-version = "1.77.2"`, inherited from Tauri 2's own requirement rather than measured. Everything is actually built and linted on 1.95.0, so **nothing has ever confirmed the code compiles on 1.77.2** — our own source may use newer features. This is the same shape as the risks this project has already been caught by: a number that looks like evidence and is really an assumption. Two honest fixes: add a CI job pinned to 1.77.2 running `cargo check`, or raise the declared version to one that is actually tested. The claim matters to distro packagers and contributors, so it should be true or absent |
| B-080 | chore | P1 | done | Add `cargo fmt --check` and `cargo clippy -D warnings` to CI | Added on the Linux runner only — formatting and lints are platform-independent, so running them three times just costs minutes. **Both already pass locally on 1.95.0 with no output**, so this guards against regression rather than cleaning anything up, which is exactly the condition this item was waiting for. Original note: | Deliberately left out of the B-054 CI workflow rather than forgotten. The AI sandbox has no Rust toolchain, so neither could be verified before committing, and a lint job that fails on its first run for cosmetic reasons teaches people to ignore CI. Add it immediately after the first successful local `cargo fmt` |
| B-081 | tech-debt | P1 | proposed | Pseudo-locale build in CI — the third B-072 guardrail | `npm run check:i18n` proves no *literal* escaped into a component; it cannot prove a translated string fits. A pseudo-locale (accented, ~35% longer, bracketed) run against the built UI catches both the strings that never reached the catalogue and the layouts that clip when German arrives. Needs something to screenshot or assert against, so it naturally follows B-024 |
| B-082 | chore | P1 | done | Pin the toolchain and commit `Cargo.lock` | `Cargo.lock` committed; CI switched to `cargo build --locked`; the CI toolchain pinned to **1.95.0** instead of floating `@stable`. **`rust-toolchain.toml` was deliberately not added.** The dev machine runs Homebrew Rust, which has no `rustup`, so that file would be honoured in CI and silently ignored locally — the precise opposite of a pin's purpose, which is making the two agree. Pinning the CI side is the half that is actually available. **Consequence to remember: the pin is now one-directional.** Bump the CI version deliberately when the local toolchain moves, or they drift apart again and CI stops representing the developer machine. Installing `rustup` (and removing Homebrew Rust) would make both halves possible — worth doing if toolchain differences ever bite. Related: B-094 | `Cargo.lock` cannot be generated in the AI sandbox (no cargo), so the first real `cargo build` produces it and it must be committed — it is an application, not a library. Pinning the toolchain makes the three CI runners agree with the developer machine, which is exactly the class of divergence B-046 exists to catch. Once both exist, switch CI to `cargo build --locked` |
| B-083 | idea | P3 | proposed | Revisit routing when the screen count grows | M1 navigates with a `useState` discriminated union in `App.tsx` — two screens do not justify a router and a desktop app has no URL bar to satisfy. Recorded so the choice is visible rather than inherited. Revisit around B-010/B-014, when there are four or more screens and back/forward starts to mean something |
| B-084 | tech-debt | P0 | done | Rebuild the shell as a desktop master–detail layout | **The two-screen model B-054 specified was wrong**, and running the app is what revealed it — it "felt like a web page, not a desktop app". A chess database is master–detail: the game list is permanent furniture and selection populates the board beside it, rather than replacing the screen. Rebuilt as one window: resizable library pane, board pane, move pane; the window is the viewport; only pane bodies scroll. Fixed three symptoms — rows reflowing on narrow windows, the board ignoring available space, and list and board never visible together. **Root cause is B-053 being skipped**, which is the document that was supposed to produce the screen list |
| B-085 | feature | P2 | proposed | Persist window geometry and pane sizes between sessions | **Scope grew with B-107:** if component windows are detachable, this has to persist *per window* keyed on a stable window identity (B-110), not just the main window's splitter — and for a streamer, a popout that forgets where it was is the difference between a scene that works on launch and one that has to be rebuilt. Original note: The splitter resets to 420px on every launch, which is the kind of small thing that makes an app feel unfinished. Needs somewhere to keep UI state — deliberately *not* the games database (ADR-0004 keeps that derivable, and window geometry is not derivable from PGN). Tauri's store plugin or a small settings file. Bundle with other preferences rather than doing it alone |
| B-093 | feature | P1 | in-progress | Mainline move navigation | Not in B-054's original scope, added because the layout could not be judged with a board that never moves, and W3 makes stepping through moves the most repetitive interaction in the product. `src/features/game/mainline.ts` walks the PGN with chessops into one FEN per ply; arrow keys, Home/End, and clicking a move all navigate. **Now covered by eleven committed tests** (`mainline.test.ts`, B-106) — the session-3 checks below were run by hand and thrown away, which verified a moment rather than the code. Two of the newly written cases initially failed, and **both failures were bad fixtures of mine rather than walker defects**. Earlier note: **Verified against fixtures** including a FEN/SetUp header, castling, and an illegal move mid-game (truncates rather than throwing). **Variations, comments and NAGs remain B-009** — this is the mainline only, and the real game tree should replace it without changing the board or panel components |
| B-086 | feature | P2 | in-progress | Keyboard navigation for the game list | Arrow keys move the selection and Enter opens the selected game in a tab, both landed with ADR-0007. Accel+W closes the active tab, routed through `src/shell/platform.ts` so the Cmd-versus-Ctrl assumption is made once (B-069). **Widened at the M1 verification pass (session 4) into owning the whole shortcut scheme, and given a concrete finding to start from: no key on the development keyboard is labelled `Home` or `End`.** The bindings themselves are confirmed working, via fn+←/fn+→ — which is how Apple keyboards without a numeric keypad emit those keys. So the code is right and the shortcut is still, for practical purposes, undiscoverable: it is reachable only by someone who already knows the fn convention. It was inherited from Windows chess software without anyone checking the target keyboard has the keys — a B-069 portability miss in a place nobody thought to look, since keyboards are not usually filed under "platform surface". Candidate fix is Cmd+←/Cmd+→ as aliases via `platform.ts`, **deliberately not applied yet**: the owner's call was to choose the bindings as one coherent scheme rather than spot-fix a key at a time, because piecemeal additions are exactly how a scheme becomes inconsistent. Not urgent — the ⏮/⏭ footer buttons already make first/last reachable. **Still open:** the scheme itself, Home/End reachability, Tab-order review, a discoverable shortcut reference. Original note: | Arrow keys to move the selection, Enter to open, Home/End to jump. Rows are currently mouse-only, which is acceptable for a skeleton and not for an application people use daily. Shortcut modifiers must go through `src/shell/platform.ts` (B-069), never `metaKey` directly |
| B-090 | tech-debt | P1 | proposed | Decide narrow-window behaviour below ~900px | A fixed 320px panel plus a fluid board plus a multi-column table stops working somewhere around 900px, and nobody has looked. Options: collapse the panel to icons, drop table columns by priority, or set a minimum window size and be honest about it. Cheap to decide, embarrassing to discover in a screenshot from a tester |
| B-091 | tech-debt | P2 | proposed | Cap the board's maximum size and decide what absorbs surplus width | A fluid board with no ceiling is absurd on a 32-inch monitor. Once capped, the leftover width becomes margin around a centred board, or a wider panel. Chess.com centres. Pairs with B-089 |
| B-092 | feature | P2 | proposed | Focus mode — hide everything except the board | Chess.com has this (plus Theatre mode) and it is the logical end of B-089's collapsibility. Cheap, and it directly serves W3, the review workflow where nothing but the board and moves matters |
| B-087 | chore | P0 | done | Choose the application layout from mocked options, before coding it | **Decided as ADR-0007 — layout C+**, after four structural options and two refinement rounds were mocked and discussed rather than coded. Inputs: `docs/ui-survey.md` (the category makes layout configurable because it is genuinely subjective), `docs/core-workflows.md` (W2/W5 want a wide list, W3 wants a dominant board, W4 wants several games open), and chess.com screenshots supplied by the owner (fluid board, fixed panel, three-part panel structure). Option E — one board with the library inside the panel — was seriously considered and rejected because a 320px panel cannot hold a filterable multi-column table. **State is modelled as a list so E stays reachable as a shell change.** Original note: | Two coded layouts were rejected in one session, which is what prompted the process change. `docs/ui-survey.md` written: the decisive finding is that **every mature product in this category makes layout user-configurable** — ChessBase ships predefined plus savable custom layouts, Scid vs. PC has five slots, and Lichess's fixed layout is its most-complained-about trait. Four options mocked (master–detail, two contexts, tabbed documents, board-dominant). **Awaiting a choice.** Plan is one default built properly, with the others as presets later — not a dockable pane system |
| B-088 | chore | P1 | proposed | Backport session-3 lessons to the AI-dev starter kit | **Session 8 adds three, and the third is a new *kind* of lesson rather than another instance.** (12) **Try to defeat a guardrail occasionally** — `check:i18n` silently missed plugin specifiers and every dynamic `import()`, both found by writing code that should have tripped it and noticing nothing happened; a guardrail nobody has attacked has unknown coverage, which is the same species as (7). (13) **Render and look at the UI, don't only build it** — a throwaway preview page plus a headless screenshot found three faults in one session, all invisible in the code and obvious in the picture, including an i18n bug open for two milestones. (14) **A permission or capability system has two failure shapes and only one is loud** — a wrong identifier is a build error, but an *incoherent permission set* (a command granted with no scope, or a scope granted with no command) is a feature that silently does nothing with everything green. Generalises past Tauri to any allowlist. **The working rule is not "check the scope" but "read the generated manifest"**: this project guessed the entry twice from its name and from a docs summary, and both guesses built cleanly and did nothing. The machine-readable list of what each permission actually grants was sitting in the repo the whole time. Original note: The kit is at 0.2.0 and these are general, not chess-specific. Three changes: (1) **mock UI layout before coding it** — new section in `ai/methodology.md`, new mandatory behavior in `AGENTS.md`; (2) **visual acceptance is not verified by a green build** — a human has to look, and the AI must say plainly what it could not verify; (3) **watch for "what applications like this do" claims that were never checked against real products.** Also worth carrying: the session-start prompt should ask what the AI *cannot* verify in its environment, since the Rust/sandbox split was rediscovered rather than anticipated. **Session 4 adds three more, all general rather than chess-specific.** (4) **`git --no-optional-locks` for every AI-side read of git state** — the "AI should avoid running git" rule was discipline without a mechanism and was broken repeatedly in one session, stranding a lock that blocked the owner's commit; the flag skips the index write the sandbox mount cannot undo, and it is verified. Also `.fuse_hidden*` belongs in the kit's `.gitignore`, since in-place edits over a mount orphan inodes in any project. (5) **A stated frequency is a measurement claim even when phrased as a design assumption** — "warnings will be common" needed a number and got a vibe; five plausible explanations were overturned by cheap checks in one session, so the kit's methodology should say to find the control before acting on the obvious culprit. (6) **When the owner challenges a claim, follow the objection through rather than conceding it** — doing so produced B-100 and the collision report, both of which a simple retraction would have missed. **Session 5 adds two, and the first is about the kit's own handover template.** (7) **"Verified against fixtures" is a claim about a moment unless the fixtures are committed** — two handover entries said exactly that, both truthfully, while the project had no test runner at all, because the verification had been done by throwaway scripts. Prose cannot distinguish a suite from a script that ran once, so the handover section should require saying *which*, and should require saying when the thing that performed a check no longer exists. Same species as (5): a sentence that reads like evidence and is really a memory. (8) **Ask for the test harness at the point the methodology first says "verify before claiming done"** — this project reached four sessions, two ADRs about correctness policy, and a backlog item explicitly requiring a cross-language corpus before anyone noticed there was nowhere to put a test |
| B-089 | feature | P2 | proposed | Layout presets, and collapsible panels | Falls directly out of `docs/ui-survey.md`. Two or three named presets capture most of the value of full configurability for a fraction of the cost of a dockable pane system. **Collapsibility is the higher priority of the two**: the single loudest complaint about the newest layout in this category is a panel that steals board space and cannot be hidden. Ties to B-085 (persisting pane sizes) |
| B-097 | feature | P2 | proposed | Import report: list the errors, with enough detail to act on | **Shrank a third time at milestone 2, and gained one hard sentence.** The error list is now of length **zero or one** — `pgn-reader`'s errors are terminal, so a single input cannot produce two. What the report must add instead is the uncomfortable part: **everything after the failure was never read**, with the byte offset to prove it. The good news is that naming the game is free — the roster tags are parsed before the movetext fails, so `White`, `Black` and `Date` are available on the error itself. Earlier: **Shrank twice in session 5 and may end up nearly empty, which is a good outcome rather than a disappointment.** ADR-0009 removed warnings and quarantine tiers, then removed legality checking from import entirely — so an import error is now exactly a game `pgn-reader` refuses. **Measured at milestone 1: that is one fixture in eighteen** (`unterminated-comment`), so this report has very little to report — which is why it dropped P1 → P2. Design it after milestone 2, when the real error set is known from real files rather than from a corpus built to be awkward. What survives regardless: errors cross IPC as codes with the frontend composing the wording (B-072); **a game's index within a file is not a stable identifier**, because an unterminated tag quote splits one damaged game into two, so identifying header text must travel with the error; and the old trap still applies — a rare event is exactly what gets implemented as a toast that disappears, so whatever list exists must be re-findable after the import finishes. Original note: **Shrank considerably under ADR-0009 (session 5), and what remains got harder rather than easier.** There are no warnings and no quarantine tier now — a game imports or it errors — so the design problem is no longer managing attention across a long tail of soft failures. It is the plainer one: **a list of errors that identifies each game well enough for a user to fix it.** That is now load-bearing in a way it was not before, because strictness removed the hiding place: ADR-0008 could attach a vague warning to a game that still appeared in the library, whereas an error means the game is absent and the message is all the user gets. "Invalid movetext in game 412" is a worse experience than the silent repair it replaced; "Game 412, Ivanov–Petrov 2019.04.02: `Lb5` is not a legal move at ply 5" is better than either. Two findings from B-099 constrain the format: **a game's index within a file is not a stable identifier**, because an unterminated tag quote splits one damaged game into two, so identifying header text must travel with the error; and error codes cross IPC while the frontend composes the wording (B-072). The trap from the earlier draft still applies — **a rare event is exactly what gets implemented as a toast that disappears** — so the list must be re-findable after the import finishes. Original note: | Falls out of ADR-0008 rule 7, and it is the rule most likely to be implemented as a number and called done. W1 requires malformed games to be *reported rather than vanishing*, so the warned and quarantined sets must be reachable and inspectable, not tallied. **Reframed on the owner's challenge (session 4), and it got easier:** the earlier draft feared warnings would be so common that presenting them would train the user to dismiss them. If clean machine exports dominate, warnings are *rare*, and a rare warning is straightforward to surface honestly — the design problem shrinks from managing attention to simply not hiding a short list. The remaining trap is the opposite one: a rare event is exactly what gets implemented as a toast that disappears. Also owns re-running a quarantined game after a parser improvement, which is cheap because dispositions are derived. Warning text is composed in the frontend from IPC codes (B-072) |
| B-102 | chore | P1 | done | Decide the chess.com import shape: JSON archives, and where metadata comes from | **Decided (session 4), on measurement rather than documentation** — a real 25-game account across 20 archive months was downloaded and inspected. **Import uses the monthly JSON**, not the `/pgn` endpoint. **The `pgn` field holds a complete per-game PGN** (21 tags, blank separator, result token), and comparing all 13 non-empty months showed the JSON carries **the identical set of games with zero content loss**, so the `/pgn` endpoint is redundant. One incidental finding: **game order differs between the two representations in 5 of 13 months**, so position-in-file must never form part of any identity. **The "JSON is a superset" claim was wrong and is corrected: neither representation contains the other.** JSON-only — `uuid`, `rules`, `accuracies`, `tcn`, `rated`, `time_class`, `initial_setup`, per-player `uuid`/`@id`. PGN-tag-only — `Event`, `Site`, `Round`, `Termination`, `Timezone`, `UTCDate`/`UTCTime`, `EndDate`, **and the ECO code**: the JSON `eco` field is an opening URL (`.../openings/Italian-Game`), equivalent to the PGN's `ECOUrl`, and `C50` appears *only* as a PGN tag. Four ADR-0005 hot fields (`event`, `site`, `round`, `eco`) are therefore unavailable from JSON. **Decision 1 — hot fields derive from the PGN tags**, by unwrapping the JSON and feeding each game's `pgn` through the same pipeline B-007 builds; only `uuid`, `rules` and `accuracies` come from JSON. One derivation path for both sources rather than two overlapping ones with a precedence rule, and chess.com's tag set happens to cover every hot field. **Decision 2 — dedupe identity is per source: `uuid` for API games, ADR-0008's content hash for files.** This is the substantive win, because it **retires the highest-regret rule in ADR-0008 for this source entirely** — the content hash exists only because file PGN has no identity, and chess.com supplies a stable one, so no false merge is possible for API games. Rule 3b also stops being blind, since `rules` is read directly. **The `eco` field was then checked properly, since decision 1 rests on it, and it is worse than "just a URL":** where both are present, JSON `eco` and PGN `ECOUrl` are byte-identical (21/21, zero mismatches), so the equivalence is real — but **`eco` is absent on 4 of 25 games whose PGN still carries `ECOUrl`**, making it strictly the less available copy. All four absences are unrated daily games, which looked like a signature until it was tested: **13 other unrated daily games do have `eco`**, so there is no discoverable rule and the omission must be treated as arbitrary. Separately, the ECO *code* is unrecoverable from the URL in practice — 23 distinct URLs mapped to 23 distinct codes here with no ambiguity, but that is 23 points against several hundred codes and a table that would break on any chess.com opening rename, whereas the `ECO` tag is present on 100% of games. **Conclusion: take both the code and the opening URL from the PGN tags; the JSON `eco` field earns nothing.** **Decision 3 — store both, as two columns.** Inspecting the 23 distinct URLs showed `ECOUrl` is *not* a prettier `ECO` but a **finer classification**: `C41` and `C47` each mapped to two different URLs identifying two different lines, so the URL distinguishes games the code cannot. `eco` earns its column for being coarse, standard and cross-source; `ecoUrl` earns its own for being more specific where it exists, and is `null` for the many sources that never emit the tag. Recorded as an addendum to ADR-0005 and added to `src/model/game.ts` and the mock data. Follow-ups: B-103 (result vocabulary), B-104 (whether to store `accuracies` at all), B-105 (opening *names*, which must come from an ECO table and **not** from prettifying the URL slug — the slug destroys apostrophes and the name's colon irrecoverably) | Cost accepted with decision 2: a game arriving both by API and by file export can import twice, because the two keys cannot see each other. Judged the better trade — a duplicate is visible and removable, a false merge is silent. Revisit if it actually happens |
| B-105 | feature | P2 | proposed | Opening *names* from an ECO-to-name table, not from the chess.com URL slug | **Raised P3 → P2 in session 7, because the trap caught its own author.** The info-panel mockup rendered the `ECOUrl` slug as "Italian Game" — two paragraphs above the same mockup's warning not to do exactly that. It was caught before any of it was coded, and the shipped panel labels the URL neutrally and derives no name at all. A recorded trap that snares the person who recorded it is one a future contributor will certainly walk into, and the cheap fix is to make the right thing available rather than to keep warning about the wrong one. Original note: The obvious way to show an opening name is to prettify the `ECOUrl` slug, and it is wrong in a way that ships visibly broken text: **the slug destroys punctuation that cannot be recovered.** `Kings-Indian-Attack` and `Birds-Defense` decode to "Kings Indian Attack" and "Birds Defense" — the apostrophes are simply gone — and the colon in "Ruy Lopez Opening: Berlin Defense" has no representation at all, since hyphens serve as both word separators and structural separators. Real slugs from the sample also carry move sequences (`Ruy-Lopez-Opening-Morphy-Defense-Arkhangelsk-Variation...11.Nbd2-h6-12.h3-Re8`), so a slug is a line identifier rather than a name. **Correct source is a standard ECO-to-name table**, which is vendor-neutral, correctly punctuated, works for every import source rather than just chess.com, and localises (B-073/B-075) — an opening name is user-facing text, so it belongs in the message catalogue's world, not scraped from a URL. Low priority: `eco` plus `ecoUrl` already identify the opening for filtering; this is about *display*. Pairs with B-023 (opening explorer), which needs names anyway |
| B-103 | tech-debt | P2 | proposed | Complete the chess.com result vocabulary before mapping it to ADR-0005's integer | `white.result`/`black.result` are per-player and carry the *termination reason*, which is richer than PGN's `1-0` — observed in the sample: `win`, `checkmated`, `resigned`. Deriving ADR-0005's integer `result` means finding which side has `win`, and treating the absence of a winner as a draw. **The sample cannot confirm that, because it contains no drawn games**, so the draw vocabulary (agreement, stalemate, repetition, insufficient material, timeout-vs-insufficient, 50-move) is entirely unobserved. **Owner decided (session 4) that guessing the draw strings is acceptable** — overriding the initial "do not guess", and reasonably: `result` is derived, so a wrong mapping costs a re-import rather than data, and B-012 is post-MVP anyway. **One condition makes the guess safe, and it is the whole substance of this item: derive the winner positively, never the draw by default.** If exactly one side reads `win`, that side won; **any other combination the code does not recognise must raise an ADR-0008 warning and leave `result` null**, rather than falling through to `Draw`. A guess that fails loudly on contact with an unseen string self-corrects on the first real drawn game; a guess that defaults to draw silently misreports every termination nobody anticipated, and scores are exactly the kind of wrong number a user trusts. Observed so far: `win`, `checkmated`, `resigned` — no drawn games in the 25-game sample, so agreement, stalemate, repetition, insufficient material, timeout-vs-insufficient and 50-move are all unobserved. Note also `rated` and `time_class` still have no ADR-0005 home |
| B-104 | chore | P1 | deferred | Store chess.com `accuracies` — decided yes, **deferred to B-012 and removed from the model (session 5)** | **The decision stands; the timing was wrong.** Re-measured session 5 across all 25 local games: `accuracies` appears in the chess.com **JSON on every game and as a PGN tag on none**. The MVP imports PGN (B-007); JSON import is B-012 and post-MVP. So `whiteAccuracy`/`blackAccuracy` were two columns **the MVP could never populate**, read by nothing — zero references outside `src/model/game.ts` and the mock data. Both removed, with a comment at the site saying why and who re-adds them. **Two entanglements go away with them, which is the reason this is recorded rather than quietly deleted.** The model has **no exceptions** again: "store the raw thing, derive the useful thing" held for every field except these, and that exception was real and correctly documented — it just did not need to exist yet. And the *retention obligation* attached to it disappears: the addendum obliged B-012 to keep source JSON so a rebuild would not lose accuracy, which made a nullable column load-bearing for the derivability condition that session 5 also judged over-formalised (B-078, deferred). Both halves now arrive at the same future moment, which is where they belong. Original note: **Decided (session 4): store them**, as `whiteAccuracy` / `blackAccuracy` columns, added to `src/model/game.ts` and the mock data, with an addendum to ADR-0005. **The interesting part is that this is the first field in the model that breaks its governing rule, and the exception is now explicit rather than latent.** "Store the raw thing, derive the useful thing" holds because every derived value is recomputable from the retained PGN; accuracy is neither raw (we did not observe it) nor derivable (we cannot recompute another engine's output) — it is *retained third-party judgement*. **The consequence is load-bearing and would have failed silently:** ADR-0004's storage gate rests on the database being derivable (B-078), so if accuracy lives only in a column while only the PGN is retained, a drop-and-rebuild loses it and derivability quietly stops being true. **Storing accuracy therefore obliges B-012 to retain the source JSON for API-imported games, not merely the PGN inside it.** B-011's migration inherits the columns; B-012 inherits the retention duty. Display rule: never render these as the application's own assessment, since B-019 will compute our own and they will disagree. Original note: | Present on 100% of the sampled games. It is chess.com's own engine analysis, i.e. **derived data produced by someone else's tool**, which sits awkwardly against ADR-0005's rule that derived values are never authoritative — we could not recompute it, so storing it means holding a number we cannot verify or reproduce. Arguments for keeping it: it is real information about the game, it is free, and discarding it contradicts "nothing is silently dropped". Arguments against: it is unverifiable, engine- and version-dependent, and B-019 will eventually compute our own accuracy figures which would then disagree with it. Cheapest honest option is probably to retain it in the tag/extras JSON without promoting it to a column or displaying it as if it were ours |
| B-100 | feature | P2 | proposed | Chess variants: select the variant named in the tag, on both sides | **Carried into ADR-0009 as rule 4, and reduced to its useful half.** A legal variant game imports normally; an unrecognised variant name is an ordinary error, as is `[Variant "Fischerandom"]` with no `FEN` — the quiet case, since nothing fails and a wrong mainline derives silently. Earlier note, from the ADR-0008 addendum (B-113), and the work changed shape: this is no longer "detect and refuse", it is "select and walk".** Confirmed against the crate docs: `shakmaty` exposes `VariantPosition` covering Chess, Antichess, Atomic, Crazyhouse, Horde, KingOfTheHill, RacingKings and ThreeCheck — the same eight `chessops` has — **behind an opt-in `variant` cargo feature**, so `src-tauri/Cargo.toml` needs `features = ["variant"]` and would otherwise fail to compile inside B-007. A supported variant with legal moves is now `clean`. What is genuinely left: an unrecognised variant name (fails symmetrically in both libraries, and currently presents to our reader exactly like an unparseable FEN), `Fischerandom` silently flattening to standard chess (new `variant_assumed_standard` warning), and normalising variant *names* across sources — lichess emits a `Variant` tag, chess.com emits a JSON `rules` field, and **the chess.com vocabulary is unobserved: all 25 sampled games say `chess`**, so that mapping must not be guessed (B-103's lesson). Original note: **The immediate half got cheaper and the rule it implements got wronger — see B-113 (session 5).** Measured: `chessops` already reads the `Variant` tag and derives variant games correctly, and `shakmaty` implements the same variants, so "eventually support the variants" is closer than this item assumed and the `variant_unsupported` warning should be reserved for variants we genuinely cannot walk. What remains genuinely broken is narrower and was not anticipated here: an *unknown* variant name returns `ERR_VARIANT` and presents identically to an unparseable FEN, and `[Variant "Fischerandom"]` silently maps to standard chess. Original note: **Found by following the owner's own premise, which is the interesting part: it is the *clean* sources that emit variants.** Lichess tags them explicitly (`[Variant "Antichess"]` and similar) and both major sites let users play them, so a variant game arrives inside a perfectly well-formed export. Walking one under standard rules yields a true and useless report — *illegal move at ply 2* — which is a misdiagnosis dressed as a finding and would send someone hunting a parser bug. **ADR-0008 rule 3b is the immediate half and belongs in B-007:** read the tag first, and for anything other than standard chess import full headers plus verbatim PGN with a `variant_unsupported` warning, deriving no mainline and making no legality claim. The game stays listed, searchable and intact; it just cannot be played through. Actually *supporting* the variants is the P2 half and is cheap when it arrives, since `shakmaty` already implements them — what matters now is not mislabelling them |
| B-101 | chore | P1 | in-progress | Measure the real condition of the owner's own PGN exports | **The instrument is built and tested: `npm run survey:pgn -- <path>`** (`scripts/survey-pgn.mjs`). **Reads `.pgn` files and chess.com `.json` monthly archives**, the latter because the JSON carries `rules`, `accuracies` and `time_class` that the `/pgn` endpoint discards — and because comparing JSON `rules` against the PGN `Variant` tag is the only way to test whether ADR-0008 rule 3b can see chess.com variants at all (B-102). JSON that is not an archive is skipped and reported rather than counted. Dependency-free Node, reads files or directories recursively, full detail always. **No redaction mode, arrived at after three corrections from the owner and worth recording because the scope narrowed each time:** first player names were hidden, then hidden behind a flag, then the flag was justified by the repo's pseudonymous identity — all aimed at the wrong target. Game data is not in scope for the repo's privacy constraint at all; that constraint covers the *developer's* footprint. Hiding names had also broken the most useful output, since a bare count of content-key collisions tests nothing when judging one means looking at the two games. **The only real leak was the absolute path, because a home directory contains a real name, and that is now handled structurally: paths print relative to the scanned input, so there is no mode to remember.** It measures exactly what ADR-0008's rules rest on: encoding and whether the Latin-1 fallback fires, roster completeness, every non-roster tag name, date shapes, results, `Variant` values (rule 3b), `%eval`/`%clk`/NAG/variation presence, unbalanced braces, headers-without-moves, unambiguous non-English piece letters (rule 4), and **content-key collisions split into same-file (candidate false merges — the rule 6 risk) versus cross-file (overlapping re-exports — the rule 6 purpose), each printed game by game so a collision can actually be judged rather than merely counted**. Verified in the sandbox against synthetic fixtures covering every metric, including both collision paths, with a negative control (a clean lichess-shaped file reports no anomalies) and a robustness pass (random binary, empty file, truncated tags, unbalanced parens — no crash, no hang). **Run over the chess.com download in session 5, and the clean-source half is now measured rather than assumed:** 40 files / 50 game records (25 games, each present as both JSON and PGN), **100% UTF-8 so the Latin-1 fallback never fires**, Seven Tag Roster complete on every game with zero duplicated tags, **every date complete** — no `????.??.??` at all — results only `1-0` and `0-1`, **no `Variant` tag anywhere**, `%clk` comments on 44, and **zero** `%eval`, NAGs, variations, unbalanced braces, headers-without-moves, or non-English piece letters. `SetUp`/`FEN` on 2. **The rule 6 number is the valuable one: 0 same-file collisions and 25 cross-file**, every one of them the same game matching across its JSON and its PGN representation — which is the content key surviving a genuinely different serialisation, not merely different line wrapping, and is better evidence for rule 6's design than the synthetic fixture was. **Two things this does not settle:** rule 3b is *untested rather than confirmed*, since the account has no variant games (the script says so explicitly), and the whole sample is clean-source, so ADR-0008 rules 4 and 5 remain unexercised. **Still to do: run it on the loose PGN files**, which only the owner can do and which is the half that carries the malformed tail. Original note: | **The evidence that should have informed ADR-0008's frequency claim and did not.** An earlier draft asserted that imports-with-warnings would be common; the owner's correction was that chess.com and lichess exports are machine-generated and clean. Both positions are theories, and **the data is sitting on the developer's own machine** — export from both accounts plus the loose files, and count: how many games, which tags actually appear beyond the Seven Tag Roster, what encoding, how many carry `%eval`/`%clk` comments, how many are variants, how much overlap between two successive exports of the same account (which is what rule 6's dedupe will face on every run). One afternoon, and it retires an argument that would otherwise be settled by whoever is more confident. **Same shape as B-077 and the B-096 control:** this project's third unmeasured claim in four sessions, and the cheapest one yet to check. Also produces realistic material for B-099 — but note that fixtures must stay invented and anonymised, so this measures the owner's files without importing anyone's name into the repo |
| B-098 | tech-debt | P2 | rejected | Notation-language detection for import (localised SAN) — **rejected by ADR-0009** | **Rejected session 5, not deferred: ADR-0009 makes localised SAN an error rather than something to interpret.** PGN mandates English SAN, so a German or French movetext file does not conform, and reporting the first offending token is both accurate and cheaper than any scan. The whole apparatus this item described — scan a file for letters unique to one convention, pick a language, apply it consistently, fall back to English-and-warn — exists only to *rescue* a non-conforming file, which is the thing the owner declined. **Everything below stays worth reading if anyone ever reopens it**, because the correctness trap is real and independent of the policy: legality cannot arbitrate between notation languages, and B-099 measured that it is worse than that — chessops's tokenizer silently rewrites `Sf3` to the legal pawn move `f3`, so the naive implementation does not merely risk the wrong reading, it produces a game nobody played. `fixtures/pgn/{german,french}-san.pgn` and `ambiguous-rd1.pgn` stay in the corpus as the evidence. Original note: | **Raised P3 → P2 by B-099's measurement (session 5), and the reason is that the failure mode is worse than this item described.** The write-up below assumed a localised token *fails* and the danger is choosing the wrong language to retry with. Measured: **it does not fail.** chessops's PGN tokenizer drops the unrecognised piece letter, so `Sf3` becomes `f3` — a legal pawn move — and a German game silently becomes a different legal game that nobody played, with no warning at all, until some later move happens to be impossible. So the harm does not require the ambiguous-position collision described below; **it happens on ordinary German and French input, immediately, and looks like success.** The priority is still not P1 because the target user's sources are canonical English, but "close to hypothetical" was too comfortable: the fixtures for it are now in the corpus (`german-san.pgn`, `french-san.pgn`, `ambiguous-rd1.pgn`) and the tokenizer behaviour is pinned by a test. One consequence for implementation: since `node.san` no longer contains the file's own text, a warning cannot be produced by comparing derived SAN against the source — the scan has to read raw movetext. Original note: **Dropped from P1 to P3 on the owner's challenge (session 4):** the dominant import sources are chess.com and lichess, which emit canonical English SAN, so localised notation is close to hypothetical for the target user and only appears in the loose-file tail. The *correctness finding* below is kept at full strength regardless of priority, because it is the kind of thing that gets implemented casually the moment someone does touch it. The concrete mechanism behind ADR-0008 rule 4 and the chess-specific half of B-073. **The obvious implementation is unsafe and it is worth recording why, because it looks correct:** "if a token fails as English SAN, try other languages and take whatever yields a legal move" collides silently — `R` is rook in English and *roi* (king) in French, `D` is queen in German and French and nothing in English. Where both a rook and a king move to `d1` are legal, `Rd1` parses under either reading and means a different game, so **legality cannot arbitrate**. Required approach: scan the whole file for letters unique to one convention (`S`/`L`/`T` German, `C`/`F` French), choose one language for the entire file, apply consistently; absent or contradictory evidence means English plus a warning. Never mix conventions within a file. Storage stays canonical English SAN |
| B-106 | chore | P0 | done | Test harness: vitest for TypeScript, `cargo test` for Rust, one shared `fixtures/` corpus | **Raised and done in session 5, because B-099 could not be started without it.** The finding that produced this item is worth more than the item: **four sessions in, this project had no test runner of any kind** — no vitest, no `*.test.ts`, no `#[cfg(test)]`, no `cargo test` in CI, no fixtures directory. It did not read that way from the handover, which described both `mainline.ts` (B-093) and `survey-pgn.mjs` (B-101) as "verified against fixtures". Both were, by throwaway scripts run once and discarded — **verification of a moment rather than of the code**, and indistinguishable from the real thing in prose. B-099 meanwhile reads as ready to start and silently assumed a harness on both sides of ADR-0008 rule 3. Same shape as the five plausible-explanation corrections in session 4, in a new place: a backlog item that looks ready and rests on something nobody checked. **Notify-and-proceed** — vitest 4 is MIT, a dev dependency, conventional, and reversible in an afternoon. Recorded in `docs/tech-stack.md`. Delivered: `npm test`/`npm run test:watch`; `test` block in `vite.config.ts` (from `vitest/config`, one config file so the `@` alias is defined once — tests must resolve imports exactly as the app does); `environment: "node"` and `globals: false`; **eleven committed tests for `mainline.ts`**, being session 3's discarded checks made permanent (standard walk, `FEN`/`SetUp` header, castling, no result token, illegal move truncating at the right ply, unusable start position, empty input, `clampPly`); `src-tauri/tests/fixtures_contract.rs`; `npm test` in the CI frontend job and `cargo test --locked` on **all three** platforms rather than Linux-only, because the Rust test asserts a relative path resolves and that is a B-069 category of divergence. **Two negative controls run, and both failed as intended:** an intentionally illegal move in the `clean` fixture failed its case, and a broken corpus path failed loudly at load rather than passing vacuously. **Writing the two tests found two bugs — both in my own fixtures, neither in the walker** (a castling fixture that moved the rook first, and an endgame FEN claiming castling rights it could not have). That is session 3's "verify the fixture before believing the failure" reproduced exactly, and it is the argument for committing tests rather than running them once: the throwaway version would have recorded a walker bug that does not exist | **The Rust half was unverifiable in the sandbox and has since been run by the owner: `cargo fmt` reformatted one statement and `cargo test` passes (1 test).** `Cargo.lock` did not change, because `serde_json` was already in the dependency graph, so the duplicate `[dev-dependencies]` entry is free and `--locked` still holds in CI. **`cargo clippy --all-targets -- -D warnings` also run and clean**, so every gate CI applies has now been faced locally: fmt, clippy, cargo test, plus the frontend's typecheck, guardrails, build and `npm test`. Nothing in this item is "should work". **One late correction worth keeping, because the test passed and was still wrong.** Re-run after B-099 grew the corpus from two fixtures to eighteen, `fixtures_contract.rs` reported the identical single green test — its only structural assertion was that the manifest is *non-empty*, so it could not distinguish two entries from eighteen. **A test blind to a 9× change in its own input is a green light carrying no information**, which is the same failure as a throwaway script called a suite, one level further in. Now asserts the manifest count equals the number of `.pgn` files on disk, mirroring the TypeScript side, so an unlisted fixture — read by nothing, asserted by nothing — fails loudly. **Small note on the formatting guess, kept because it is the reusable part:** writing Rust that must satisfy `cargo fmt --check` without being able to run rustfmt meant predicting where it would break long chains, and the prediction was wrong in exactly one place — it broke after the `=` rather than inside the chain. So the working rule for AI-written Rust in this repo is not "format it correctly", it is **"expect one `cargo fmt` pass and budget for it"** |
| B-099 | chore | P1 | done | Malformed-PGN fixture corpus, shared between Rust and TypeScript | **Done in session 5: eighteen fixtures covering ADR-0008 rules 1, 2, 3, 3b, 4, 5 and 6**, with a test asserting that exact rule set is covered so the corpus cannot go stale unnoticed. Rule 7 is the import report — UI, B-097, nothing to assert against a file. **`expected.json` separates two kinds of field, and the split is the honest part:** `plies` and `truncatedAtPly` are *observed* — measured from the reader and asserted today — while `disposition` and `warnings` are *specification* that only B-007 can assert. Recording them together is deliberate; pretending the second pair is verified would repeat B-106's mistake in a new place. `plies` is asserted separately because the worst failures lose moves while reporting success. **Method note that is the whole argument for doing this before B-007: every expectation was measured before it was written**, and three of eighteen fixtures behaved differently from what the ADR would have led anyone to write. An importer built first would have been written to the ADR's assumptions and these would have looked like correct behaviour. **Three findings, in descending order of importance.** (1) **Localised SAN is not rejected, it is silently reinterpreted** — `Sf3` does not fail; the PGN *tokenizer* drops the unrecognised piece letter and yields `f3`, a legal pawn move, so a German game becomes a different legal game nobody played, with no warning, until a later `Lb5` truncates it. Rule 4 argued legality cannot arbitrate between notation languages; measured, it is worse — the corruption precedes legality and `node.san` no longer holds the file's text, so a warning cannot be derived by comparing our SAN to the source without re-reading raw bytes. **B-098 upgraded P3 → P2 on this.** (2) **chessops honours the `Variant` tag**, so rule 3b's premise is false — see B-113. (3) **An unsupported variant is indistinguishable from a broken position** (`ERR_VARIANT` surfaces as `truncatedAtPly: 0`, same as an unparseable FEN), and `[Variant "Fischerandom"]` silently maps to standard chess, so a Chess960 game with no FEN derives a plausible wrong mainline with no error. Two of the surprises are pinned as their own tests, so if chessops changes them the ADR gets re-read. **Also measured and recorded per fixture:** an unterminated comment swallows four plies and the result token while reporting no truncation; an unterminated tag quote turns one damaged game into *two* games, so an import report can legitimately say "4 of 3"; chessops fills missing roster tags with `?`/`*` so a missing tag is indistinguishable from a literal `"?"`; duplicate tags resolve **first-one-wins** (nothing says pgn-reader agrees — a B-064 divergence candidate with no illegal move in sight); and the movetext's final result token **overwrites** a contradicting `Result` tag. Original note: ADR-0008 is unverifiable without one, and it is also the cheap guard for B-064. Needs a case per rule: unterminated comments and quotes, missing and duplicated tag-roster entries, `Result` contradicting the final token, Latin-1 bytes in a file with no declaration, German and French SAN, an ambiguous `Rd1` that is legal under both readings, an illegal move mid-game, headers with no moves, and bytes that are only PGN by extension. **The corpus must be shared rather than duplicated per language**, because ADR-0008 rule 3 has `shakmaty` and `chessops` walking the same mainline: a game shakmaty accepts and chessops truncates presents as `clean` and then visibly stops mid-game, which is B-064's risk becoming user-visible. Mock fixtures already carry the awkward *header* cases (accents, Cyrillic, one player written two ways, unknown dates); this extends the same habit to malformed move text. Invented names only | **Three more bad fixtures of mine, and the third is instructive:** a castling test that moved the rook first, an endgame FEN claiming castling rights it could not have, and an Antichess game whose moves were not legal Antichess either — the last being the worst, because it truncated at ply 2 and therefore appeared to *confirm* the rule-3b misdiagnosis the ADR predicted. **A fixture that fails for the wrong reason is more dangerous than one that fails for no reason**, since it corroborates whatever you already believed. Cure was ten seconds per position against chessops before asserting |
| B-114 | chore | P0 | done | Decision Gate: import fidelity, revisited — **ADR-0009, strict import** | **Owner rejected ADR-0008 as disproportionate (session 5), and the evidence backs that rather than the ADR.** ADR-0009 supersedes ADR-0008 rules 1, 2, 3, 4, 5 and 7; rule 6 was already gone and rule 3b survives restated. **Four rules replace seven: a game imports or it errors; nothing is repaired; **the libraries are the validator and we add nothing of our own**; variants are selected from the tag.** Two facts decided it, and neither is the frequency argument that ADR-0008 was previously challenged over. **First, the only real data anyone has looked at is spotless** — B-101's survey of the chess.com corpus found zero anomalies of any kind, and the malformed tail rules 4 and 5 existed for has *still* never been measured. **Second, ADR-0008's own safety argument was weaker than it looked.** It justified permissiveness with "repair cannot corrupt and rejection cannot lose", because verbatim PGN sits in the row — but the stronger fact is outside the database: **the PGN file is still on the user's disk**, so refusing a game loses nothing the filesystem had not already kept. Permissiveness was free in safety and expensive in complexity, and only the first half had been weighed. The clinching case is `german-san.pgn`: under ADR-0008 a German file imported with a heuristic warning attached to a game that *was never played*, because the tokenizer rewrites `Sf3` to `f3`. Under ADR-0009 it errors — **though not because we detect the substitution**, which we explicitly do not; it errors because a later token is one the library will not play. **Two follow-up corrections from the owner cut deeper than the first pass.** *(a)* "We cannot change the behaviour of dependencies — use their own error handling and validation." My draft had listed conditions the libraries *accept* (duplicate tags, `Result` disagreeing with the termination marker, missing roster tags), which we would have had to check ourselves — a validator by another name. Removed, along with the single exception I had argued for, a zero-moves guard to keep a PNG file out of the library, which was the same mistake in smaller clothing. **So only 4 of 18 fixtures are errors** and a non-PGN file imports as one empty junk row; everything the libraries tolerate is now an entry in ADR-0009's **"Accepted risks"** table, seven of them, each measured with a stated reason. *(b)* Derivability was over-formalised — see B-078, downgraded to P3. **What the simplification actually buys, item by item:** B-098 (notation-language detection) **rejected outright** rather than deferred; `src/model/game.ts` never gains the disposition and warning columns ADR-0008 required, so B-011's migration stays smaller and — usefully — there is nothing to unwind, since they were specified but never added; B-097 shrinks from "surface rare warnings without training the user to dismiss them" to "list the errors"; B-100 keeps only variant *selection*; and **B-064's shared assertion becomes boolean** — the two rule implementations now only have to agree on whether a game is valid, not on the ply at which a mainline truncates. The B-099 corpus was **repurposed rather than wasted**: all eighteen fixtures kept, **4 expected errors and 14 expected imports** after correction (a) above, `disposition`/`warnings` replaced by `outcome`/`errorCode`, and every tolerated-but-non-conforming case annotated as an accepted risk rather than left looking like an oversight. The rule-coverage test earned its keep by failing loudly the moment seven rules became four | **The accepted cost, stated plainly so it can be revisited on evidence rather than feel:** games in the irreplaceable tail — club games, arbiter exports, hand-typed files — are now refused rather than half-understood. That is defensible because the file survives on disk and the error names the game, and **B-101's un-run loose-file half is exactly the measurement that could reopen it**, which makes that item more interesting than it was. If it turns up a large tail of near-miss files, the answer is a new decision, not repair rules smuggled in underneath this one. Second cost: **diagnostic quality is now load-bearing** — "invalid movetext in game 412" is a worse experience than a silent repair, where ADR-0008 could hide behind a disposition |
| B-113 | chore | P1 | done | ADR-0008 rule 3b rewritten, and rule 6 removed from the MVP — addendum accepted | **Decided session 5, and the owner's challenge changed the bigger half of it.** Two changes, both written into ADR-0008 as an accepted addendum. **(1) Rule 3b superseded: select the variant explicitly on both sides.** Verified against the crate docs rather than assumed — `shakmaty` exposes `VariantPosition` with the same eight rule sets `chessops` has (Chess, Antichess, Atomic, Crazyhouse, Horde, KingOfTheHill, RacingKings, ThreeCheck), **behind an opt-in `variant` cargo feature** that `src-tauri/Cargo.toml` will need and would otherwise have surfaced as a compile error inside B-007. So a supported variant whose moves are legal under its own rules is now `clean`, exactly like a standard game, and `variant_unsupported` narrows to the two cases where it is true: an unrecognised variant name (`ParseVariantError` in shakmaty, `ERR_VARIANT` in chessops — symmetric), and a new `variant_assumed_standard` for names accepted by flattening, which is what `Fischerandom` does. **The reason the old text was wrong is the useful part: its premise was true of Rust and false of TypeScript.** shakmaty requires the caller to pick a variant, chessops reads the tag — two libraries this project deliberately pairs, opposite defaults, same tag, which is exactly B-064. Suspending derivation would have frozen that asymmetry into the specification and left the database refusing to derive a game the UI plays through in full. Two consequences recorded while free: **variant identity is part of position identity**, so B-042's index either keys on variant or B-018 excludes variant games — mixing Antichess and standard positions returns matches that mean nothing; and a legality claim now spans eight rule sets, so B-064's shared corpus matters more rather than less. **(2) Rule 6 removed from the MVP, on the owner's challenge that nothing in the MVP needs to hash a game.** Correct, and the ADR's own reasoning does not survive it: the hash served idempotent re-import, which served ADR-0004's derivability condition (B-078) — but **derivability needs only that dropping the database and rebuilding from retained PGN reproduces it**, which holds with or without dedupe, since each source file is read once per rebuild. Dedupe is the different property of importing *the same file twice into an existing database*. Conflating those two is what made a content hash look load-bearing, and that conflation was mine. So B-007 assigns stable row IDs and no content key; duplicate rows are possible, **visible and removable** — which is the failure mode this ADR already said it preferred over a silent false merge, now obtained by not building the mechanism. Dedupe becomes B-022's problem, where merging is the subject anyway and a user can confirm it. **The methodological point is the one to keep:** a hole had been found in the key (the Seven Tag Roster omits `Variant`, so an Antichess and a standard game with identical headers and moves hash identically) and my instinct was to patch the key. Deleting it was better, and **the question that got there was "why are we hashing at all?" rather than "is the hash correct?" — the second question cannot reach the first** Earlier note, kept because the shape recurs: **Found by B-099's fixtures (session 5), and it is a measurement against an *accepted* ADR rather than a draft.** Rule 3b says: read the `Variant` tag first, because walking a variant game under standard rules produces "a perfectly true and perfectly useless report — *illegal move at ply 2*". **Measured, that does not happen.** `chessops`'s `startingPosition()` reads the `Variant` header itself and returns the correct variant position, so `mainline.ts` derives variant games *correctly* — verified across Antichess, Crazyhouse, Atomic, Three-check and King of the Hill (own rules), plus Horde and Racing Kings (own starting positions); a Crazyhouse drop `P@e4` plays as a drop, pocket visible in the FEN. **The rule should survive, but its justification and its behaviour both need revising, and the reason is a divergence the rule would create rather than prevent:** rule 3b instructs the importer to derive no mainline and make no legality claim, while the frontend that displays the game can play the whole thing through. That is exactly B-064 — shakmaty and chessops disagreeing about the same game — except arrived at *by specification*, which is worse than a bug because it is intentional and would be defended. `shakmaty` implements the same variants, so the cheap fix is probably to derive the mainline under the named variant on both sides and reserve the `variant_unsupported` warning for variants we genuinely cannot walk. **Two branches where rule 3b is still exactly right, and they are the ones the ADR did not anticipate:** an unknown variant name returns `ERR_VARIANT`, which our reader surfaces as `truncatedAtPly: 0` — indistinguishable from an unparseable FEN, so the misdiagnosis the rule feared is real but arrives through a different door; and `[Variant "Fischerandom"]` silently maps to standard chess, which is quieter still, since nothing fails. Needs an ADR-0008 addendum (the ADR is accepted, so this is a decision rather than an edit) and touches B-100's implementation half. **Sixth time on this project that measurement has overturned reasoning, and the first time the reasoning was in an accepted ADR** | Cheap to decide, and cheapest right now: no importer exists yet, so changing the rule costs a paragraph. After B-007 it costs a paragraph plus code plus a re-import |
| B-096 | tech-debt | P2 | rejected | Board appears to lag a live window resize — **investigated and closed: it is the webview, not our code** | Raised at the M1 verification pass (session 4): dragging the window wider with a game tab open makes everything right of the board stretch elastically, then snap back when the drag stops. Settled layout correct, no scrollbars, so repaint timing rather than layout. **Two candidates, and the reported one was ruled out by reading the CSS** — the side panel cannot move, because `.game-view` declares `grid-template-columns: minmax(0, 1fr) var(--panel-w)` and its width is pure CSS. That left the board, sized in pixels from JavaScript by the `ResizeObserver` in `useChessground.ts`, versus macOS simply compositing a stretched copy of the last painted frame during live resize. **Settled with a control rather than a diagnosis, per B-077: the library tab contains no JavaScript-sized element and stretches and snaps identically.** So no code of ours participates, and `useChessground.ts` is exonerated rather than merely unaccused. Nothing to fix; **do not "improve" the observer or move the board to `aspect-ratio` on the strength of this symptom** — the latter is what B-069 deliberately rejected as untested on WebKitGTK (B-066), and adopting it on macOS evidence alone would trade a non-problem for an unmeasured risk | Kept because the *method* is the reusable part: the JavaScript-over-CSS sizing trade was the obvious suspect and was innocent, the control was free and available the whole time, and it converted a suspicion about our own architecture into a closed question in one window drag. Second time B-077's habit has paid for itself |
| B-107 | feature | P3 | proposed | Detachable component windows, so OBS captures a window instead of a screen region | **The streaming use case, and the priority is a holding value — see B-112 before scheduling any of B-107 – B-111.** Streamers composing a scene in OBS currently have to draw display-capture *regions* around parts of an app window, which is fiddly to set up, breaks whenever the app layout moves or the window is resized, and captures whatever else happens to be underneath. Window capture has none of those problems. So: let the board, the player/clock strip, the eval bar, the move list and the game info each be popped out into their own small window, positioned once and captured individually. **The architecture already permits this, and that is not luck — it is ADR-0007's rule that no component below `App.tsx` knows its own placement.** A popout is exactly "render this component into a different container", which is the same property that keeps the simpler "option E" shell reachable. Two consequences worth stating: this **raises the value of that constraint** (breaking it makes popouts expensive, not just option E unreachable), and it is a **second reason `useChessground.ts` must stay the only React↔chessground seam**, since a detached board is a second mount of it. Related: B-092 (focus mode) is the in-app cousin and probably shares the component work; B-089 (presets/collapsibility); B-085 (window geometry, which becomes multi-window geometry); B-024 (a chroma-key colour is a theme token). **The eval bar cannot precede B-014**, since there is no engine output to show yet |
| B-108 | chore | P3 | proposed | Decision Gate: multi-window architecture — where windows are created, and how state is shared | **Hard-stop tier, and it must precede any B-107 code.** Two questions, and the second is the real one. (1) **Window creation is a Tauri API**, so it belongs behind `src/shell/` per ADR-0001 — the standing Electron-portability constraint means no component may reach for `WebviewWindow` directly. (2) **Shared state across webviews is a genuine architectural change, not a feature.** Today the open-games list is `useState` in `App.tsx` (B-083); a second window is a second webview with its own JavaScript context, so a popped-out board cannot read that state at all. Options span a wide cost range: keep the state in Rust and treat every window as a subscriber (fits B-067's "parse in Rust, keep latest state, emit on a timer" shape, and is the version that also serves an engine feed); adopt a frontend store synchronised over Tauri events; or restrict popouts to read-only projections that receive a FEN and a clock and nothing else. **The third is the cheapest and covers the streaming case entirely**, which is worth noticing before anyone builds a state layer for it — a broadcast overlay does not need to be interactive. Decide with the gate's six-part format; it touches platform surface *and* the state model |
| B-109 | feature | P3 | proposed | Broadcast-friendly window properties: chroma key, borderless, aspect lock, integral scaling | What separates "a detached window" from "a window a streamer can actually use". **Chroma key first, transparency only if it earns it** — a solid, configurable background colour is boring, fully portable, works in every capture path, and is what most scenes key out anyway. **True per-pixel transparency carries a claim worth checking before anyone promises it: Tauri's transparent windows on macOS are documented as requiring the `macos-private-api` feature, and enabling private APIs is understood to make an app ineligible for the App Store.** That is recorded as *unverified* on purpose — it is exactly the kind of plausible-sounding constraint this project has been wrong about before (see risk 9), and it wants confirming against Tauri's own docs rather than repeating. If true it is a B-032 packaging/distribution constraint, not a UI one. The rest are ordinary and cheap: borderless/no-chrome, optional always-on-top, a position lock so an accidental drag cannot ruin a live scene, and **aspect-ratio lock with integral board sizing** — the board is already floored to a multiple of 8 in `useChessground.ts`, and captured-then-rescaled fractional pixels are visibly soft, which is the whole reason a streamer would prefer window capture in the first place |
| B-110 | tech-debt | P3 | proposed | Window identity must be stable, because OBS binds a source to a window title | **The non-obvious trap in the whole idea, and the one most likely to be discovered live on stream.** OBS matches a window-capture source by executable plus window title, so **any title that changes at runtime silently breaks a scene the streamer set up earlier** — a popped-out board titled after the current game would rebind or go blank on the next game. Titles must therefore be stable identifiers of the *component*, not descriptions of the *content*, and must not drift between launches. This collides with two existing constraints in an interesting way: **window titles are user-facing strings** (B-072), so they belong in the message catalogue — yet a localised title is by definition not stable across locales, and changing the app language would break every OBS source the user configured. Likely answer is a stable, unlocalised identifier in the title alongside localised text, but it is a real design question rather than a detail. Also relevant to B-085: window identity is what geometry is persisted against |
| B-111 | chore | P3 | proposed | Window capture is platform-divergent, and the divergence is worst where we have no testers | Recorded so the platform half of B-107 is not discovered at release. Window capture is not one mechanism: macOS requires screen-recording permission and has its own window-capture path, Windows has several with different behaviour around borderless and transparent windows, and **Linux is the hard one — Wayland restricts capture through the portal/PipeWire, which historically made per-window capture unreliable or unavailable depending on compositor.** Nothing here can be settled by reasoning, and this project's own record on unmeasured platform claims is poor (risk 9). It is also **the exact intersection of the two things we cannot currently test** — B-070 (no Windows or Linux testers) and B-066 (WebKitGTK unverified) — so an honest MVP-adjacent version of B-107 would ship macOS-first per B-068 and say plainly that the other two are unverified rather than implying parity |
| B-112 | chore | P3 | proposed | Vision question: are chess streamers an audience this product serves? | **Answer this before assigning real priorities to B-107 – B-111 — it is what prices them, and it is cheap.** `docs/product-vision.md` describes a local-first personal chess *database*: import, search, organise, annotate, analyse your own games. Streaming overlays serve a different job for a different person — a broadcaster presenting to an audience — and it is the **first request in five sessions that adds an audience rather than a capability.** That is not an objection: it is plausibly a strong differentiator, since the incumbents in the survey (`docs/ui-survey.md`) are not built for it, and the underlying work is largely reusable (a popped-out board is B-092's focus mode with a window boundary). But the vision currently neither includes nor excludes it, and an unstated audience is how scope grows without anyone deciding. Three honest outcomes: amend the vision to name broadcasting as a supported use case; keep it as a post-MVP opportunistic feature that must never bend the database design; or record it in `docs/parking-lot.md` as an adjacent product. **Whichever it is, the MVP is unaffected** — nothing in B-107 – B-111 touches import, storage or search, which is the reason all six can sit at P3 without blocking anything |
| B-115 | tech-debt | P2 | proposed | **KNOWN ISSUE:** a non-English PGN imports as a game nobody played, silently — report dropped move tokens | **Owner's decision (session 5): assume all PGN input is English SAN, and flag better error handling for a later release.** Recorded as a known issue rather than a bug because the behaviour is understood, measured, and deliberately not fixed in the MVP. **What actually happens, measured at B-007 milestone 1 — and the two libraries fail differently, which is the part worth knowing.** For `1. e4 e5 2. Sf3 Sc6 3. Lb5 a6` (German: Springer, Läufer): `pgn-reader` **drops** every token it cannot parse, yielding `e4 e5 a6` — black's `a6` becomes white's third move. `chessops` **rewrites** them, `Sf3` becoming the legal pawn move `f3`, yielding `e4 e5 f3 c6` before truncating at `Lb5`. So the row imports with `plyCount` 3, the board plays through a different four-ply game, the two numbers disagree, and **neither library reports anything**. PGN mandates English SAN, so such a file is non-conforming — but nothing detects that today. **The fix for a later release is error handling, not language detection, and that distinction is what makes this cheap where B-098 was expensive.** We do not need to know which language a file is in — only that the parser silently discarded part of it. **Compare the parser's token count against the movetext's own move-token count**: a mismatch means tokens were dropped, needs no language table, no per-token guessing and no heuristics, and catches every other silent-drop case as a bonus rather than just this one. It is a check of ours, which is why ADR-0009 keeps it out of the MVP — but when it arrives it should be that, not the language scan B-098 described. Fixtures already exist: `fixtures/pgn/{german,french}-san.pgn`, with the measured numbers for both libraries in `expected.json`. Recorded in ADR-0009 under "Stated assumption: PGN input is English SAN" | Deliberately P2 rather than P3: it is the only case in ADR-0009's risk table where a *silently wrong* game reaches the library, as opposed to a missing or empty one. Everything else the policy tolerates is visible |
| B-117 | tech-debt | P2 | done | Open external links through `src/shell/`, not with a bare anchor | **Done (session 8), folded into B-007 milestone 4 because both needed the same `capabilities/` file.** `src/shell/opener.ts` wraps `tauri-plugin-opener`; the info panel's `ECOUrl` is now a `<button>` styled as a link rather than an anchor, because an anchor is the thing that navigates the app window away. **One deliberate addition beyond the item as written: the opener refuses anything that is not `http:`/`https:`.** That URL came out of a PGN tag written by whoever produced the file, and handing arbitrary text to the OS shell is a different question from deciding what a legal move is — so it does not contradict ADR-0009 rule 3. **Shipped broken and took two wrong fixes to correct, which is the part worth keeping.** The
capability first read `opener:allow-open-url` alone — clicking did nothing. Changed to
`opener:allow-default-urls` alone — clicking still did nothing. **Both build green, because Tauri
validates permission *identifiers* at build time and does not check that the set you listed is
coherent.** The answer, read from `src-tauri/gen/schemas/acl-manifests.json` (generated by
`tauri-build` from the plugins' own manifests, and the authoritative local source): `allow-open-url`
grants the **command** and no scope; `allow-default-urls` grants the **scope** and its
`commands.allow` is empty. **Both are required, and neither is a broader version of the other.** The
correct entry is both, deliberately not the plugin's own `opener:default` set, which adds
`allow-reveal-item-in-dir` — opening Finder on a path is not something this app should be able to do.
**Two lessons, and the second is the transferable one.** (a) The scope glob question that made me
avoid a hand-written scope was moot: the plugin itself ships `https://*` and it matches URLs with
paths. (b) **A capability system has two failure shapes and only one is loud** — wrong identifier is
a build error; an incoherent set is a feature that silently does nothing. **And when a capability is
inert, read the generated ACL manifest instead of reasoning about the entry name.** I guessed twice
before doing that, which is exactly the habit risk 9 exists to prevent, in a new place. Caught only
because the owner clicked it — a green build, 86 passing tests, a rendered blue link and a
screenshot pass all agreed it worked.
**Still plain text: the `Link` tag inside the All-tags disclosure** — that is B-121. Earlier: **Raised at B-007 milestone 3, where the first external URL arrived.** chess.com games carry `ECOUrl` and `Link`, and the info panel shows both as **text rather than links**, on purpose: in a Tauri webview a bare `<a href>` navigates the *app window* away from the app, which is a worse failure than plain text. The fix is Tauri's opener plugin behind `src/shell/`, per ADR-0001 — the same boundary that keeps the frontend Electron-portable — plus a capability entry. Not urgent: the URL is selectable and copyable today. **Do not "just add target=_blank"**; that is the version that breaks, and this note exists so the next person does not rediscover it in the running app. |
| B-116 | tech-debt | P3 | deferred | Import resynchronisation: carry on after a refused game instead of stopping | **Raised and deliberately not built, session 6, and the deferral is the decision.** Measured at B-007 milestone 2: `pgn-reader`'s errors are irrecoverable, so an unterminated `{` near the top of a large export loses every game after it. Recovery would be a forward scan for the next `[Event ` at a line start, restarting the reader there — perhaps fifteen lines. **It does not violate ADR-0009 rule 3**, since every recovered game is still parsed in full by the library; what it adds is our own judgement about where a game *begins*, permanently, for an event **nobody has counted**. **B-101's un-run half is that count, and it is the trigger for this item**: if loose files contain unterminated comments, build it; if they do not, this stays deferred forever. Pinned meanwhile by `the_games_after_an_unterminated_comment_are_lost_and_that_is_measured` in `src-tauri/src/import/mod.rs`, so whoever changes their mind has to change a test that says why. Precedent it follows: the same "cheap to add later, expensive to remove" asymmetry that deleted ADR-0008's content hash |
| B-118 | tech-debt | P1 | proposed | The session-start documents are growing without a mechanism for retiring anything | **`docs/handover.md` is 1,446 lines / 116 KB and `docs/backlog.md` is 111 KB, and `AGENTS.md` instructs every session to read both first.** That is the cost of starting work, paid every session, rising monotonically — and it is invisible because each individual addition is justified. This is the same shape as the risks this project keeps catching: a number nobody has looked at. **What makes it urgent rather than tidy is the stated success condition:** the handover says "this file plus `docs/backlog.md` should be sufficient — if the next session has to ask something that was settled here, this handover failed". A document that cannot be read in full stops being sufficient before it stops being complete. **Proposed fix, cheap and reversible:** the handover keeps the last two sessions in full and moves older per-session narrative to `docs/sessions/NN.md`, linked; the backlog's `done` rows keep the decision and its reasoning and lose the blow-by-blow of how it was reached. Nothing is deleted — the append-only rule is about the record, not about one file. **Do not fix this by writing less.** The detail is what makes these sessions resumable; the problem is that it all lives on the critical path |
| B-119 | chore | P2 | proposed | Render the UI in a headless browser so visual acceptance stops depending entirely on the owner | **Raised because it was tried and worked (session 8).** A throwaway Vite entry point rendering the import surfaces with fabricated reports, screenshotted through Playwright against the container's preinstalled Chromium, **found three faults a green build could not: unformatted numbers (B-072), "0 games" on a file that never opened, and failure lines that read as siblings of the file name they belonged to.** All three were invisible in the code and obvious in the picture. **This does not replace the human — it is not the Tauri webview, the data was fabricated, and nothing here judges whether a layout is *good*.** What it does is move the mechanical half of "a green build does not verify a layout" off the owner's desk, so what reaches them is a screenshot to react to rather than a description to imagine. Two natural forms: a committed preview route with fixture-driven states, and screenshots posted in the handover. **It is also what B-081 has been waiting for** — the pseudo-locale build "needs something to screenshot or assert against", and this is that thing. Costs a dev-dependency on Playwright, which was deliberately *not* left in the tree this session because nothing yet uses it |
| B-120 | feature | P3 | proposed | A drag-over affordance, so a drop looks possible before it happens | Milestone 4 handles Tauri's `drop` and ignores `enter`/`over`/`leave`. So the window gives no feedback while files are held over it, and the only confirmation that a drop is possible is the dialog appearing after the fact. The dialog is the *decision* surface and that part is right; this is the smaller question of whether the window looks receptive. Cheap — the events are already delivered by the same subscription — and deliberately deferred rather than forgotten, because it is polish and the drop itself works |
| B-121 | tech-debt | P3 | proposed | Make URLs inside the All-tags disclosure open too | B-117 routed the info panel's `ECOUrl` through `src/shell/opener.ts`. The disclosure below it still renders every tag as plain text, so chess.com's `Link` — the URL of the actual game, arguably the more useful of the two — is copyable but not clickable. Left alone on purpose: the disclosure is a raw-tag dump where every value is untyped text, and deciding *which* of them are URLs is a small classifier that wants a moment's thought rather than a regex bolted on at the end. The opener already refuses non-`http(s)` schemes, so the safety half is done |
| B-122 | feature | P3 | proposed | Revisit the single-game import flow — can it be shorter? | **Owner's note after testing B-007 milestone 4 (session 8): it works and it is fine for the MVP, but it may be one step longer than it needs to be.** Dropping a file with one game in it currently goes drop → dialog listing the file → Import → result step offering *Open game* → the game opens: four surfaces to read one game. The two ends of the design space are both defensible and neither is obviously right, which is why this is a revisit rather than a fix. **Straight through** — a single game opens immediately, no dialog, with the strip recording it — is what Lichess does, and it is only safe *because* a single game is the only thing Lichess imports; here it means a drop sometimes opens a tab and sometimes does not, which is a rule the user has to infer. **Keep the confirmation, drop the offer** — import silently and let them open it from the table — is more consistent but throws away the thing they most likely wanted. A third option nobody has costed: skip the *result* step for a lone clean game and open the tab straight from Import, so the offer disappears rather than the confirmation. **Do not decide this from the armchair** — it is a feel question, and the owner is the only one who can answer it, having now used it. Related: B-120 (drag affordance) is the other polish item on the same gesture |
| B-123 | bug | P1 | done | Pasting PGN on a game tab did nothing | **Found by the owner testing the build (session 8), fixed the same session. Trivial tier.** The `paste` listener was declared inside `LibraryView`, so it existed only while the library tab was mounted — pasting with a game open silently did nothing at all. **Not a decision anybody made and not recorded anywhere as intended behaviour; purely a consequence of where the effect was written**, dating from milestone 3 when the library was the only place a paste could land. Moved to `App.tsx`, which is always mounted, and it now follows the same path a dropped file takes: return to the library, open the dialog prefilled, wait for Import. One guard added — **if the dialog is already open the listener stands down**, because the dialog's own textarea is the right target and hijacking the gesture would discard whatever is staged. **The transferable bit is small and worth having: a global gesture declared inside a view is only global while that view is mounted**, and the failure is silence, which is the hardest kind to notice. Milestone 4 added a *second* global gesture (drag-drop) and put it in `App.tsx` for reasons of its own; that the two then disagreed is what made this visible |
| B-124 | chore | P2 | proposed | Point the fixture corpus at the person testing the app | **Raised session 8 from a small failure that is entirely ours.** After exercising the happy path the owner reported being unable to test the unhappy paths "as I do not have sample data" — and `fixtures/pgn/` has held nineteen purpose-built malformed and awkward files since B-099, committed to this repo before the importer existed. The corpus was built to be asserted against by `cargo test`, `fixtures/README.md` documents it as a contract for *adding* fixtures, and **nothing anywhere frames it as a set of files to drag onto the running app.** So the material existed, the person who needed it did not know, and the gap cost a testing pass. **The fix is a pointer, not a feature:** a short "trying it by hand" section in `fixtures/README.md` naming the three or four highest-value files and what each should visibly do, plus a line in the handover's verification list. **The general form is worth more than the instance** — an artefact built for one audience (the test runner) is invisible to another (the person at the keyboard) unless something says so, and this project has now made that mistake twice: session 5's "verified against fixtures" meant a script nobody could re-run, and this is the same distance between a thing existing and a thing being usable. Cheap, and it makes every future milestone's manual pass better rather than just this one |
| B-077 | chore | P2 | proposed | Performance criteria must be median/p95 plus a control, never an absolute max | Lesson from B-048: the agreed "max frame < 16.7 ms" threshold was unattainable by construction at 60 Hz vsync, and macOS Low Power Mode silently capped the machine to 30 fps, nearly producing a false negative against ADR-0001. A plain-browser control on the same machine is what caught it. Record power state and Low Power Mode with any benchmark |

## Rejected / deferred (kept on purpose)

| ID | Type | Status | Description | Why |
|-------|---------|----------|-------------|-----|
| B-034 | idea | rejected | Build our own chess engine | Explicit non-goal. This is a GUI; engines are a solved, specialised problem |
| B-035 | idea | rejected | Become an all-in-one online chess platform (play vs. humans, lobbies, ratings) | Explicit non-goal in the vision. Chess.com/Lichess own this |
| B-036 | idea | deferred | Cloud sync / hosted account for the database | Conflicts with local-first data-ownership stance; revisit only if multi-device demand is real. Would trigger auth + infra gates |
| B-037 | idea | deferred | Mobile / tablet clients | Cross-platform means Windows, macOS, Linux desktop for now |
| B-038 | idea | deferred | Web version of the app | Same reason; also complicates local engine execution |
| B-039 | idea | deferred | Social features: sharing, comments, public collections | Out of scope; the product is a personal library |
